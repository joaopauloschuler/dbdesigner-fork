# Model editing bug catalog (tables, columns, relations)

Date: 2026-09-08. Branch: a3. Status: final (30-minute exploratory session on the real display, DISPLAY=:0 / XWayland; see Coverage). Fixed so far: #5 (8208145), #1, #6, #8 (670864e), #2, #4, #7 (3d2c6f3), #9 (83f1ec8), #11, #12 (c79715a), #13 (2930524). Round-5 verification: see the end of this file.

Scope: creating/editing/deleting tables, columns and relations in the model editor, the Table Editor, Relation Editor, persistence (save/reload) and the generated SQL (MySQL / SQLite). Earlier catalogs (ui-bug-catalog.md, db-ui-bug-catalog.md, sqlite-/mysql-bug-catalog.md) are not repeated; regressions are marked as such. Screenshots: `<scratchpad>/shots/model-edit/`.

## Entries

### 1. Table Editor, Indices page: the column list of the selected index is empty (PRIMARY shows no `idproductgroup`)

Severity: functional (minor - display only, the index itself is intact in the model).
Steps: open order.xml; double-click the `productgroup` table header; the Indices page is shown with `PRIMARY` selected in the index list.
Observed: the right-hand "index columns" list box is empty although the PRIMARY index contains `idproductgroup` (`08-tableeditor.png`). Same after adding/renaming (`10-newcol-dt.png`). No stderr output.
Expected: the columns of the selected index listed (as in the original DBDesigner 4), so that columns can be removed with the eraser button.
Suspected cause: the index-columns list is filled in the index-list `OnClick`/`RefreshIndexColumns` path of `src/EditorTable.pas`; the LCL `TListBox` under GTK2 is probably filled before the page/handle exists or the selection-change event that triggers the fill does not fire on programmatic `ItemIndex` (compare db-ui #19). Fix scope: small.
Status: fixed in 670864e - the list was filled (probe: 1 item) but painted white on white: `ShowIndex` set `IndexColListBox.Color:=clWindow`, which the GTK2 LCL applies to the NORMAL/ACTIVE/PRELIGHT base of the tree view, and GTK2 draws the pre-selected row of an *unfocused* list in the ACTIVE state (white text) - a click into the list (SELECTED, orange) made it appear. Now `clDefault`/`clBtnFace` (theme colours). Adding a column via the grid popup "Add Column(s) to Selected Index", changing the kind to UNIQUE INDEX (mouse pick in the dropdown), removing it with the eraser and OK + reopen all verified (`fix01-06-08/20-30`). See notes-to-myself.md "Fix: model-edit #1, #6, #8".

### 2. Canvas keeps a stale horizontal line after a table is renamed/resized through the Table Editor

Severity: cosmetic.
Steps: open order.xml; double-click `productgroup`; change the Table Name to `productgroup_renamed` (table becomes wider); OK.
Observed: the table, the DB Model tree and the `ProductgroupRel` line update correctly, but a light grey 1-px horizontal line remains across the whole `OnlineStore` region at canvas y~503 (from the region's left edge to its right edge, `12-main-after.png`). It is not part of any object and disappears on a full repaint.
Expected: no leftover pixels after the editor closes.
Suspected cause: the invalidate rectangle after `EditorTable` closes (`TEERTable.RefreshObj`/`DoPaint` in `src/EERModel.pas`) covers the old table bounds only, while the region background is redrawn with an off-by-one row (or the selection frame of the table is cleared with the region's colour minus one line). Fix scope: small.

Status: fixed in 3d2c6f3 - not an invalidation problem: the white 190-px line at y=502 is the `RelEnd` paintbox of the *splitted* `forumpost` relation (onlinecustomer -> forumpost). `TEERRel.PaintObj2Canvas_RelStart/_RelMiddle/_RelEnd` read `width`/`height` inside `with RelEnd do with theCanvas do`, which the LCL resolves to `TCanvas.Width/Height` (GDK drawable size 3072, or 0 before the handle exists). `TEERRel.DoPaint` paints RelEnd through `RelEnd.Canvas`: the first `width` read gives 0 (`w:=-1`), `MoveTo` allocates the handle, `LineTo(xo+width-1-w)` then draws the white "clear" pass from -1 to 3072 clipped to the control, and the black dash-dot-dot pass lands off-clip. It shows after the Table Editor OK because that path repaints directly outside an expose. The painters now capture the control size first; the splitted stubs and end icons appear too. Verified with two renames and a table drag (`fix02-04-07/30-fixed1/2`, `31-moved.png`). See notes-to-myself.md "Fix: model-edit #2, #4, #7".

### 3. Return in the Table Editor's "Table Name" edit closes the dialog (acts as OK)

Severity: cosmetic / usability (minor).
Steps: Table Editor open; click into Table Name, type a name, press Return.
Observed: the dialog closes and the change is applied (`11-rename.png` was taken before Return, `12-main-after.png` after). Tab from Table Name goes to the Table Prefix combo, where typed text is ignored silently.
Expected: original behaviour is the same (OK is the default button), so this is only listed for completeness - not a defect unless the caller wants Return to move to the grid. Fix scope: n/a.

### 4. Relation Editor: Foreign Keys grid clips the source column name and draws the "Comment" header cell black

Severity: cosmetic (minor).
Steps: order.xml; double-click the `CartRel` relation label.
Observed: the `Source Column` cell shows `idonlinecustome` (last letter clipped, column too narrow for the text), and the `Comment` header cell has a black background band on its right/top edge (`13-releditor.png`). Name, kind (1:n), visibility, source/destination tables, FK mapping, Reference Definition (RESTRICT/RESTRICT) are all shown correctly.
Expected: columns wide enough for the longest name (or auto-sized), uniform header background.
Suspected cause: fixed `ColWidths` in `src/EditorRelation.lfm` sized for the Windows font; header drawn in `OnDrawCell` of `src/EditorRelation.pas` with a partly uninitialised brush. Fix scope: small.

Status: fixed in 3d2c6f3 - there is no `OnDrawCell`; the black band is the LCL `tsLazarus` title bevel (`cl3DDKShadow` right/bottom edge of every fixed cell, most visible on the last column). `FKGrid` is now `Flat` with silver fixed/border lines and `SetRelation` runs `AutoAdjustColumns` (old widths as minimums, Comment column fills the rest): `idonlinecustomer` is complete (`fix02-04-07/43-releditor-final.png`).

### 5. Objects created in the running session (tables from the Table tool, relations from the relation tools) cannot be selected or edited with the mouse; objects loaded from the file can

Severity: functional (major for model editing - a table created with the tool cannot be opened in the Table Editor, a new relation cannot be opened in the Relation Editor or deleted).
Steps: order.xml; click the Table tool (palette, 10th button); click on empty canvas at ~(958,180) and again at ~(958,430) - `Table_15` and `Table_16` appear and are added to the DB Model tree (`16-crop.png`); select the Pointer tool; double-click the `Table_15` header (3 attempts, mouse moved onto the header first, `xdotool click --repeat 2`). Then: 1:n relation tool (12th button); click `News`, click `Employee` - `Rel_12` is drawn and `FKidNewsCol (FK)` is added to `Employee` (`27-crop.png`; the FK name is correct, order.xml has `FKPrefix="FK" FKPostfix="Col"`); Pointer tool; click on the `Rel_12` line and on its label; Edit > Delete selected Object(s) (Ctrl+Del).
Observed: `Table_15`: no selection frame, no Table Editor (`21-crop.png`, `22-crop.png`); the same double-click on `productgroup` immediately afterwards opens its Table Editor (`24-dt.png`). `Rel_12`: the double-click on its label opens the *Region Editor* of the `Forum` region underneath (`31-regioneditor.png`), the click on the line selects nothing and the delete confirmation lists no object (`39-confirms.png`, top; "Yes" changes nothing - 6 px difference between `33-selrel.png` and `35-afterdel.png`). The same probe on the loaded `CartRel` lists `CartRel`, and on `productgroup` lists `productgroup, ProductgroupRel` (`39-confirms.png`). No stderr output.
Expected: new objects behave like loaded ones - selectable, editable by double-click, deletable.
Suspected cause: every EER object is a control with its own `OnMouseDown` (`TEERObj.Create`, `src/EERModel.pas:~7273`; relation parts at ~10349-10428). `TEERModel.NewTable` (`src/EERModel.pas:2626`) and `NewRelation` (`:2670`) call `SendRegionsToBack; theTbl.Show; theTbl.BringToFront` after the model form is already shown; under LCL GTK2 the z-order change is not applied to the GTK layout children (or the new control's widget is realised without the button-press event mask), so the region / the model's `GridPaintBox` (`:1312`) stays above the new control and receives the clicks. Objects created during XML load (before the form is shown) get the right order. Check whether the objects become selectable after Save + reopen (not tested). Fix scope: medium (z-order handling in `TEERObj`/`SendRegionsToBack`, or an explicit hit-test in `TEERModel.DoMouseDown`).
Status: fixed in 8208145 - not reproducible with clicks placed on the objects: with `writeln` probes in the mouse handlers every session-created table/relation control sat at the top of `Parent.Controls` and received its clicks (inside and outside a region, on order.xml and on a File > New model; single click selects, double-click opens the Table/Relation Editor, Ctrl+Del lists and removes the relation plus its FK column). The reported symptoms match clicks that landed beside the very small targets (the News->Employee line is ~13 px long and 1 px wide inside a 14-px control, the label 16 px high): a click on the region background deselects everything (empty Ctrl+Del list, #7) and a double-click there opens the Region Editor. The real defect found on the way: `TEERModel.SendRegionsToBack` set `ComponentIndex`, which is a no-op for LCL z-order, so the guarantee `NewTable`/`NewRelation`/`NewNote` relied on did not exist; it now really sends the regions and the `GridPaintBox` to the back and `LoadFromFile2` applies it after every XML load (paste, undo, plugin import, appended model). See notes-to-myself.md "Fix: model-edit #5".

### 6. Table Editor column grid: Tab inside the in-place Column Name editor does not move to DataType - the next keystrokes are appended to the name

Severity: functional (minor). Regression of the navigation part of db-ui #16 (826a071 says Tab goes Column Name -> DataType).
Steps: Table Editor of `productgroup`; click the Column Name cell of the empty last row; type `abc`; press Tab; type `DECIMAL(10,2)`.
Observed: the cell contains `abcDECIMAL(10,2)` (`24-dt.png`); Tab did nothing visible. Return then commits the name and opens the datatype in-place combo with `INTEGER`; Ctrl+A + `DECIMAL(10,2)` + Return is accepted, the flags column switches to ZEROFILL and the next row's editor opens (`25-dt2.png`); the canvas shows the new column after OK (`26-crop.png`). The first-character doubling itself is fixed (`abc` -> `abc`).
Expected: Tab commits the name and moves to the DataType cell.
Suspected cause: the fix in `ColumnGridKeyDown` (`src/EditorTable.pas`) handles Tab only while the grid itself has the focus; while `EditorTableFieldEdit` (`src/EditorTableField.pas`) is active the Tab is swallowed by that control (GTK2 `TEdit` with no `OnKeyDown` forwarding) - Return is forwarded, Tab is not. Fix scope: small.
Status: fixed in 670864e - VK_TAB did reach `TEditorTableFieldEdit.DoKeyDown` (probe) and was ignored there; the LCL's tab navigation did not move the focus either. Tab/Shift+Tab now commit the cell (`ApplyChanges(goRight/goLeft)`, the constants existed but were unimplemented), consume the key and feed VK_TAB/VK_LEFT into `ColumnGridKeyDown`, so the cursor moves like the grid's own Tab/Left; a new row continues with the datatype editor, and Tab in the datatype editor of the last row opens the next row's name editor (Shift+Tab goes back to Column Name). Verified `abc` + Tab -> datatype editor with `abc` (no doubling), `VARCHAR(10)` + Tab -> next row, `g` + Tab -> DataType cell, Return + Shift+Tab -> Column Name (`fix01-06-08/31-35`, `60-62`).

### 7. Ctrl+Del with nothing selected shows the delete confirmation with an empty object list

Severity: cosmetic (minor).
Steps: click on empty canvas (or on an object that cannot be selected, see #5); press Ctrl+Del.
Observed: "Are you sure you want to delete the selected Objects? The following Objects will be deleted:" with an empty list (`30-confirm.png`, `34-confirm.png`); Yes does nothing.
Expected: nothing happens (the Edit menu item is disabled by `DeleteMIShow` when nothing is selected, but the shortcut bypasses the `OnShow`-time check).
Suspected cause: `TMainForm.DeleteMIClick` (`src/Main.pas:~950-990`) does not test `ObjectList.Count=0` before `MessageDlg`. Fix scope: small.

Status: fixed in 3d2c6f3 - `DeleteMIClick` shows the confirmation only for a non-empty list; click on empty canvas + Ctrl+Del opens nothing.

### 8. Table Editor: after renaming a column in the grid the OK button disappears - the edit cannot be confirmed

Severity: functional.
Steps: double-click `News`; click the `idNews` Column Name cell (twice - the first click after window activation is lost); type `x` (the in-place editor opens and replaces the name); End, `2`, Return.
Observed: the grid shows `x2` (`43-comb.png`, middle), but the green OK check mark at the bottom right is gone - only the red Cancel X is painted (`44-editor-still.png`); two clicks on the OK position do nothing; Cancel closes the dialog. The canvas keeps `idNews` and `Employee.FKidNewsCol` (`46-crop.png`), so PK-rename propagation to FK columns could not be evaluated. Compare `25-dt2.png`: after adding a *new* column (name + datatype) both buttons are present and OK works. Re-check on `productgroup`: renaming the non-key `groupname` (`g` + Return, `50-comb.png`) and the PK `idproductgroup` (`p` + Return, `53-comb.png`, top) both keep the OK button; End inside the grid moves to the Comments cell (`53-comb.png`, bottom). So the trigger is specific to the News sequence - either the End key pressed while the in-place name editor is open, or the collapsed (►) display state of `News`. Reproduced once; needs the exact sequence above.
Expected: OK stays visible; the rename is applied and the FK column of `Employee` is renamed to `FKx2Col` (order.xml has FK prefix/postfix).
Suspected cause: the only code that hides the button is `SubmitBtn.Visible:=False` for read-only / linked tables (`src/EditorTable.pas:463-466`), which does not apply here; the button is most likely covered or destroyed by the in-place `EditorTableFieldEdit` / datatype combo teardown - the sequence "Return in the name editor of an *existing* row" differs from the new-row path (`ColumnGridKeyDown`, `src/EditorTable.pas:~1013`, and the `EditCellStr` callback that ends editing). A `TSpeedButton` under GTK2 that loses its parent panel's repaint after a child control is freed would explain a painted-away *and* unclickable button only if the control is really hidden, so check `SubmitBtn.Visible/Enabled` in the rename path first. Fix scope: small-medium.
Status: fixed in 670864e - the rename was not the trigger: the OK button is already absent when the editor of `News` opens (`fix01-06-08/05-te-news.png`). `News` and `Employee` are *linked objects* in order.xml (`IsLinkedObject="1"`, placed from the `bookshop` model listed in `<LINKEDMODELS>`; blue border, no header bitmap on the canvas) and `src/EditorTable.pas:463-466` hides `SubmitBtn` for linked objects and read-only models while `FormClose` discards the edits - original semantics. `productgroup` kept the button because it is not linked. The editor now shows `Linked object from model "bookshop" - read only, changes cannot be applied.` (new `ReadOnlyLbl`), sets the button state explicitly and refuses the in-place edits, toggles, insert/delete and index changes for such tables (`40-41`). PK-rename propagation, verified on `productgroup` -> `product` instead (News/Employee cannot be edited): `ApplyChanges` ran `CheckAllRelations` before the relations rebuilt their PK -> FK mapping, so `product` lost `idproductgroup (FK)` and got no replacement until the next check (`50-52`); `SourceEERTable.RefreshRelations` now runs first and `pg` + Return + OK yields `product.FKpgCol (FK)` immediately with the canvas repainted (`53-canvas-after-fix.png`).

## Not bugs / could not reproduce

- Double-click on a table header opens the Table Editor and double-click on a relation label opens the Relation Editor; both close with the OK/Cancel speed buttons (ui-bug #22 fix holds).
- Renaming a table through the Table Editor: canvas text, table width, the attached relation line and the DB Model palette tree all update (`12-main-after.png`) - apart from the stray line in #2.
- The Table Editor grid shows key/NN/AI icons and the datatype icons (ui-bug #11 fix holds); the first-character doubling (db-ui #16) could not be re-tested because the click into the new-column row was lost (see below).
- File menu keyboard navigation (`alt+f`, Down x7, Return) landed on "Close" instead of "Save As" and left dead override-redirect popup windows that swallowed all further input (`06.png`, `07-newtables.png` show the resulting empty main window). This is an xdotool/GTK2 menu driving problem (submenus open on Down), not an application bug; the app had to be restarted.
- FK columns created by a new relation are named `FK<pk>Col` - correct, order.xml carries `FKPrefix="FK" FKPostfix="Col"`.
- Delete confirmation for a table lists the table and its relations (`productgroup, ProductgroupRel`) - correct cascade.
- No exceptions, GLib criticals or Pango warnings on stderr during the session (only the canberra-gtk-module message).

## Coverage

Exercised (real display, `<scratchpad>/shots/model-edit/`): opening order.xml; Table Editor of `productgroup` (Columns grid, Indices page, Table Name edit, rename + OK, canvas repaint, DB Model tree refresh, new column via typing, in-place datatype editor with DECIMAL(10,2), canvas after OK); creating two tables with the Table tool (canvas + tree refresh); Relation Editor of `CartRel` (all fields inspected, Cancel); creating a 1:n relation News -> Employee with the tool (line, label, FK column); selection/delete probing of `Rel_12`, `CartRel`, `productgroup` via the delete confirmation (Escape); Table Editor of `News`: rename of the PK column in the grid (blocked by #8).

Not reached in the 30-minute limit (part of the budget was lost to a mis-routed click that switched the main window into Query mode and to stuck File-menu popups): in-place rename, move/resize, copy/paste/delete/undo, table options pages (Table Options, Advanced, Standard Inserts, Comments), prefixes/colours, collapse/expand; datatype palette drag, ENUM parameters, NN/AI/PK toggles, default values, reordering, deleting columns that take part in relations/indices, Column Parameters dialog, creating/deleting indices; non-identifying / n:m / 1:1 / self relations, changing relation kind, FK propagation on PK changes (blocked by #8), deleting relations and tables with relations, line dragging; Save/Save As/reload persistence; SQL Create Script export (MySQL/SQLite); Navigator/zoom/context menus.

Recommended next round: start the app, immediately do File > Save As with the mouse (File menu popup is at +42+95, 212x399; "Save As ..." is the 9th row including separators) and never use keyboard menu navigation; the main-window client origin is +42+69 when the window is moved to 0,0 (the mode-toggle button sits at client (18,49), any click there flips to Query mode).

## Pass 2 entries

Date: 2026-09-08, second 30-minute pass (real display). Screenshots: `<scratchpad>/shots/model-edit/pass2/`. Model: order.xml saved as `<scratchpad>/p2.xml` right after opening.

### 9. Edit menu: Copy / Cut / Paste / Select All Objects / Center Model are permanently disabled; Ctrl+C / Ctrl+V do nothing

Severity: functional (copy/paste of tables is impossible; "Select All" and "Center Model" unreachable).
Steps: open order.xml; click the `productgroup` header (selected, dashed frame); Edit menu.
Observed: `Copy selected Object(s)`, `Copy ... as Image`, `Cut`, `Paste`, `Select All Objects`, `Center Model` are greyed with a table selected (`16-editmenu-sel.png`) and without (`13-editmenu.png`); only Undo/Redo/Delete are enabled. Ctrl+C then Ctrl+V with `productgroup` selected changes nothing on the canvas (`15-paste.png`). Undo via Ctrl+Z and Redo via the menu work for a table move (`10-12-comb.png`, `14-redo-menu.png`).
Expected: the items enabled while a model is active (Copy/Cut with a selection, Paste with clipboard content), as in DBDesigner 4 where the Edit menu is refreshed on open.
Suspected cause: `src/Main.lfm:3203-3253` declare the items with `Enabled = False` and nothing in `src/` ever sets `CopyMI/CutMI/PasteMI/SelectAllMI/CenterModelMI.Enabled` (grep `\.(Enabled|Visible)` over `src/` has no hit for them); the Delphi original enabled them in an `OnClick` of the top-level `EditMI` (the LCL `EditMI` has no handler, `src/Main.lfm:3183`). The handlers `CopyMIClick`/`PasteMIClick` exist (`src/Main.pas:1298`, `:1340`-ish). No shortcuts are declared for Copy/Paste/Undo/Redo either (`ShortCut` only on DeleteMI 16430 = Ctrl+Del and one more), so Ctrl+C/V go nowhere; Ctrl+Z is handled elsewhere (works).
Suggested fix scope: small - add an `EditMI.OnClick` (or `OnMenuShow`-like refresh in `DeleteMIShow`'s place) that enables the items from `GetSelectedObjsCount>0` / clipboard state / `FActiveEERForm<>nil`, and give Copy/Cut/Paste/Undo/Redo their standard shortcuts.
Status: fixed in 83f1ec8 - the CLX form refreshed these items through `TMenuItem.OnShow` (`DeleteMIShow`, `PasteMIShow`, `ActivateEERMIOnShow`, `Undo/RedoMIShow` in `src/Main.xfm`), which the LCL does not have, so the port lost the refresh and the items stayed at their streamed `Enabled=False`. `RefreshEditMenuItems` now runs from `EditMI.OnClick` (GTK2 fires it when the submenu opens) and from the new `MainForm.OnShortCut`; Undo/Redo/Copy/Cut/Paste/Select All got Ctrl+Z/Y/C/X/V/A, disabled while an edit control has the focus so the keys reach the control (verified: Ctrl+A/C/V in the Table Editor's name box edits text, the canvas gets nothing). The shortcut refresh deliberately does not query the clipboard - the GTK2 nested loop in `Clipboard.AsText` eats the Ctrl release before the LCL evaluates the shortcut. Pasted tables get unique names (`productgroup_1`), Center Model scrolls the view to the centred model, Copy as Image is guarded (external clipboard check not possible on this box). Verified on the real display (`fix09/04`, `09-crop`, `10-editor-copy`, `11-12`, `13-14`, `15-16`, `21`); self-test 93 PASS / 0 FAIL / 78 SKIP. See notes-to-myself.md "Fix: model-edit #9".

### 10. Table Editor OK after adding a second PK column: the table's bottom row is clipped on the canvas (height not recomputed)

Severity: cosmetic.
Steps: double-click `product`; click the key cell of `name` (becomes PK, NN, AI toggled via the flag cells); Comments page; OK.
Observed: `name` moves under `idproduct` and the children get `FKnameCol (FK)` (correct, `29-after-ok.png`), but the `product` table is now cut off at the bottom: `pic` is half visible and the bottom border is gone (compare `01-main.png`). The PK separator line is drawn below the two key columns.
Expected: the table control grows to hold all rows.
Suspected cause: `TEERTable.RefreshObj`/`PaintObj` height calculation in `src/EERModel.pas` does not add the extra pixels for the PK/non-PK separator when the PK count changes, or the control's `Height` is set before the columns are re-sorted (`ApplyChanges` in `src/EditorTable.pas` sorts PK columns first after the size was computed). Fix scope: small.
Status: not a bug, verified in c79715a - probes in `RefreshObj`/`PaintObj2Canvas` show `ColCount=7 Obj_H=142 Height=106` (75 % zoom) before and after the PK toggle, and the cached image, the control and the row positions are identical (`fix10-12/p2-cmp.png` overlays the catalog's own `01-main`/`29-after-ok`). The "missing border" is the dotted selection frame that `PaintObj2Canvas` draws at `yo+tblHeight-3`, on top of the solid border, while the editor leaves the table selected; `pic` is fully drawn. The height/width logic was exercised anyway: PK removed again, a 38-character column added (200x119) and deleted (back to 110x106) - the control follows the rows and the longest name in every case (`12-crop2.png`, `16-crop2.png`). No code change. See notes-to-myself.md "Fix: model-edit #10-#12".

### 11. n:m relation tool: the generated join table is dropped on top of existing tables

Severity: cosmetic (minor).
Steps: n:m tool (palette centre y=360); click `productgroup`, click `creditcard`.
Observed: `productgroup_has_creditcard` with `FKidproductgroupCol (FK)` / `FKidcreditcardCol (FK)` and `Rel_13`/`Rel_14` are created correctly, but the table is placed at the midpoint of the two parents, over `carthasproduct` and the "Stores all products..." note (`54-nm.png`). Its columns are not marked as PK (no key icon) although an n:m join table normally gets a composite PK of the two FKs.
Expected: a free spot near the midpoint, FK columns as PK (check `TEERModel.NewRelation`/`nmTable` creation in `src/EERModel.pas`, search `_has_`).
Suggested fix scope: small (position search); the PK question needs comparison with DBDesigner 4 semantics.
Status: fixed in c79715a - new `TEERModel.GetFreeObjPos` (ring search around the midpoint, position-grid step when the grid is on, tables/notes/images with a 10-px margin as obstacles, canvas bounds respected) is used by the n:m branch of `TEERTable.DoMouseDown` after the join table has its real size; `productgroup_has_creditcard` now lands in the free gap above the note (`fix10-12/21-crop.png` vs `18-crop.png`). The PK part was a misread: the original creates both relations as identifying `rk_1n` and `CheckRelations` marks the FK columns `PrimaryKey`; the canvas shows key icons for both (`18-nm-zoom.png`), so nothing was restored.

### 12. Table Editor, Table Options page: the "Row format" combo is too narrow ("defau" clipped)

Severity: cosmetic (minor).
Steps: Table Editor of `product`; tree node Table Options.
Observed: the combo at the bottom right of "How Settings" shows `defau` (`25-tableopts.png`); the Password field shows the password in clear text (`theproducts...`). All other fields readable.
Expected: combo wide enough for "default"/"dynamic"/"fixed"/"compressed".
Suspected cause: fixed `Width` in `src/EditorTable.lfm` (`RowFormatCBox` or similar) sized for the Windows font. Fix scope: trivial.
Status: fixed in c79715a - `RowFormatLU` widened 75 -> 160 px and the "Row Settings" group box moved from `Top = -2` to `Top = 0` (the LCL caption was clipped to "How Settings" by the tab sheet); `23-crop.png`. The Password field stays in clear text: the original `src/EditorTable.xfm` has no `EchoMode`/`PasswordChar` on `TblPasswordEd` either, and the value is the MySQL `PASSWORD=` table option written in clear text to the model XML.

## Pass 2 not bugs / verified OK

- Table move by dragging the header, Ctrl+Z undo and Redo (menu) restore the position (`10-12-comb.png`, `14-redo-menu.png`).
- Deleting a table with a relation (`productgroup`): confirmation lists `productgroup, ProductgroupRel`, the relation and the child FK column `product.idproductgroup (FK)` are removed and the DB Model tree is refreshed (`20-21.png`); Ctrl+Z restores table, relation and FK column (`22-undo-del.png`).
- Table Editor flag cells: clicking NN / AI / the key cell of `name` toggles them (`24-27.png`); OK propagates the new second PK column to both children (`carthasproduct.FKnameCol`, `onlineorderhasproduct.FKnameCol`, `29-after-ok.png`) and the columns persist in the saved XML (`PrimaryKey="1" NotNull="1" AutoInc="1"`, two `FKnameCol`).
- Table Options / Advanced / Standard Inserts / Comments pages display the stored values (Next Auto-Increment 100, Checksum, Chunks 2/64 kB). Typing into the Comments memo works when the memo is really clicked (client y~380 of the 491-px dialog) and the comment is saved (`Comments="cmt p2"` in p2.xml after File > Save).
- Double-click on an already selected table opens its Table Editor.
- Ctrl+Del on a selected table opens the confirmation (needs the window to be activated first; the first click after activation is lost - driver issue).
- No exceptions or GLib criticals on stderr in the whole session (`stderr.log`: only the canberra message).
- Driving note (not an app bug): a Table Editor whose Cancel click was lost stays open as a modal and silently swallows all clicks on the palette and canvas for the rest of the session - always `waitgone` the dialog and re-check `xdotool search --name "Table Editor"` before reporting "tool does not respond". Palette centres: Table y=293, 1:n identifying 315, 1:1 non-id 338, n:m 360, 1:1 390, 1:n 413... (lower group 390/413/435), Pointer 80; File > Save is the 7th popup row (screen 92,243 with the window at 0,0).

## Pass 2 coverage

Exercised (`shots/model-edit/pass2/`): Save As into the scratchpad; table drag/undo/redo; Edit menu state with and without selection (#9); Ctrl+C/V; delete table with relation + undo; Table Editor of `product`/`productgroup`: NN/AI/PK toggles, second PK column propagation (#10), Table Options/Advanced/Comments pages (#12), comment persistence via File > Save; n:m relation creation (#11).

Not reached in 30 minutes (about 8 minutes were lost to a Table Editor left open by a lost Cancel click and to wrong palette coordinates): in-place rename, resize, table prefix/colour options, collapse/expand, table context menu; datatype palette drag, ENUM/SET parameters, VARCHAR length change, default values, column reorder, deleting a column that is in an index / relation, duplicate names, Column Parameters dialog, creating/deleting a two-column index; 1:1 and self relations (one attempt of a self 1:n relation on `productgroup` created nothing, `55-self.png` - unconfirmed, the second click may have been lost), Relation Editor kind change and ON DELETE/UPDATE options, deleting a relation, dragging line segments/labels, splitted display; persistence of the n:m join table (the last File > Save click did not register twice, p2.xml timestamp 14:05 - unverified), reload of p2.xml, SQL Create Script export (MySQL/SQLite); Navigator repaint/zoom, DB Model tree after n:m, context menus, Datatypes palette. Only opened at the end: File > Export > SQL Create Script dialog (`62-sqlexport-*.png`, target SQLite preselected, the SQLite-specific option labels are greyed with grey check marks - not evaluated) on a fresh restart that reopened bin/Examples/order.xml (not p2.xml), so the DDL of the edited model was not generated. WorkMode=1 and git status (only this file) confirmed; no stderr output in the second session either.

## Verification findings

Date: 2026-09-08, verification pass on the rebuilt branch (commits 8208145..8d6559c). Screenshots: `<scratchpad>/shots/model-edit/verify/`; scratch model `<scratchpad>/model-edit/verify/v5.xml` (order.xml saved there right after opening), scripts `v5-mysql.sql` / `v5-sqlite.sql`.

### 13. Relation Editor: the "Dest. Name" column of the Foreign Keys grid is scrolled out of view on the second relation opened in a session

Severity: functional (minor - the FK column names cannot be seen or edited in the editor).
Steps: open order.xml; double-click the `CartRel` label (all three grid columns shown, `13-releditor.png`); Cancel; create a 1:n relation with the tool and double-click its line (or open any other relation).
Observed: the grid shows only `Source Column | Comment`, the `Dest. Name` column (`FKidCol`) is missing (`24-re-after-click.png`, `25-re-clickid.png`); a click into the fixed `Source Column` does not bring it back.
Cause / fix: the reused form keeps the focused cell of the previous relation; with the cursor in the Comment column the LCL grid (FixedCols=1) scrolls `LeftCol` to 2 so the focused column is fully visible, which pushes column 1 out. `SetRelation` now resets `LeftCol`/`Col`/`Row` after sizing the columns. Verified: `CartRel` then `ProductgroupRel` in one session show `Source Column | Dest. Name | Comment` with `xy` / `FKxyCol` (`7072.png`). Status: fixed in 2930524.

### 14. GLib-GObject-CRITICAL "no emission of signal key-press-event to stop" on Return in the Table Editor grid

Severity: cosmetic (stderr noise only; nothing visible misbehaves).
Steps: Table Editor of `productgroup`; click the `idproductgroup` Column Name cell; press Return (the in-place editor opens with the name selected); type `xy`; Return (commits).
Observed: two `GLib-GObject-CRITICAL **: ../../../gobject/gsignal.c:1184: no emission of signal "key-press-event" to stop for instance '0x...'` lines at the moment of the Return keys (`stderr-session1.log`, 15:09:37); the rename itself works (`10.png`). Round 4 had seen the same message once for Escape in the datatype editor and could not reproduce it; this sequence reproduces it.
Suspected cause: `ColumnGridKeyDown` / `TEditorTableFieldEdit.DoKeyDown` set `Key:=0` for VK_RETURN and the GTK2 LCL then calls `g_signal_stop_emission_by_name` on a widget that is not inside a `key-press-event` emission (the key was re-dispatched to the editor created during the handler). Not fixed - would need a probe with `G_DEBUG=fatal-criticals` and a backtrace; low value.

Observations (not entries): the `Rel_13` label of an n:m join relation lands under the parent table (`carthasproduct`) after a reload (`5051.png`) - label placement, same as the original; the Standard Inserts stored in the model keep the old table/column names after a rename (they are free text in DBDesigner 4 too), so the exported inserts for `productgroup`/`product` fail on load - DDL unaffected; the Navigator's blue viewport frame looks narrower than the visible canvas (about half) - not investigated, present before this round (`00-main.png`).

## Verification (round 5)

| Entry | Result | Note |
|---|---|---|
| 1 | verified | PRIMARY shows `idproductgroup` in the index-column list; two-column index `nmst` on `Table_16` built via the grid popup, column removed from it when the column is deleted (`03-te-pg`, `44c`, `45`) |
| 2 | verified | rename `productgroup` -> `productgroup_renamed` + OK: no stale line, splitted `forumpost` stubs painted (`12d`) |
| 3 | not-verifiable | n/a by design (Return = OK); not re-driven |
| 4 | verified | `CartRel` grid shows `idonlinecustomer` complete, flat silver header (`13s`); new #13 found and fixed on the second relation |
| 5 | verified | `Table_16` from the Table tool selects and opens its editor; 1:n tool `Table_16` -> `creditcard`: line click selects, double-click opens the Relation Editor, Ctrl+Del lists `Rel_12` and removes it with `FKidCol` (`17`, `2223`, `3031`) |
| 6 | verified | `abc` + Tab -> datatype editor, `VARCHAR(30)` + Tab -> next row; same for `id`/`nm`/`st` (`0405`, `18`, `3840`) |
| 7 | verified | click on empty canvas + Ctrl+Del opens nothing |
| 8 | verified | PK rename `idproductgroup` -> `xy` + OK gives `product.FKxyCol (FK)` at once; the linked `News` editor was not re-driven (`12c`) |
| 9 | verified | Edit menu with a table selected: Copy/Cut/Paste/Delete/Select All/Center Model enabled with shortcuts; Ctrl+C/Ctrl+V -> `productgroup_renamed_1`, status "1 Object(s) pasted", Ctrl+Del removes it (`1415`, `16-confirm`) |
| 10 | verified | second PK `name` on `product`: table fully drawn after deselecting, `pic` and bottom border intact (`33z`) |
| 11 | verified | n:m `productgroup_renamed` x `creditcard`: join table in the free gap above the note, both FK columns with key icons, composite PK in the DDL (`34c`, `v5-mysql.sql`) |
| 12 | verified | Table Options page: "Row Settings" caption complete, `default` readable in the Row format combo (`07-tableopts`) |

Sweep (all OK unless noted): collapse/expand via the header arrow, table context menu (`3537`); ENUM('a','b') datatype, default value `a`, duplicate name auto-renamed `nm_2`, column delete with Delete key (`3840`, `45`); Relation Editor kind 1:n non-id -> 1:1 (end marker and FK becomes PK), Create Reference Definition + ON DELETE CASCADE persisted (`RefDef="Matching=0\nOnDelete=1\nOnUpdate=3"`, `2829`, `29z`); Save + fresh start on `v5.xml`: every edit present (`5051`); SQL Create Script for SQLite and MySQL: 14 tables load cleanly (`sqlite3`, `mysql` into `dbdtest5`, dropped) - renamed table, `xy` PK, `FKxyCol`, `PRIMARY KEY(idproduct, name)`, `Table_16` with `INDEX nmst(nm)`, join table `PRIMARY KEY(FKxyCol, FKidcreditcardCol)`, no `Rel_12`; only the stored Standard Inserts with old names fail (see observations). Regressions: Table Editor first character not doubled (db-ui #16), export dialog per-target options switch SQLite/MySQL (db-ui #10), DataImporter and SimpleWebFront launch from the Plugins menu (`57`). Not reached: datatype palette drag (one attempt dropped beside the table), column reorder, Column Parameters dialog, self relation, relation line/label dragging, reverse-engineering placement (db-ui #5, needs a database session), in-place rename/resize (not features of the original). Self-test 93 PASS / 0 FAIL / 78 SKIP; `TestSQLExprShim`, `TestSQLite`, `TestMySQLShim` print SUCCESS.

## Round 6 (2026-09-08): blank model, two tables, typed+commented fields

Date: 2026-09-08 (30-minute time box, real display DISPLAY=:0, binary built from f0000a1). Screenshots: `<scratchpad>/shots/round6/`; saved model `<scratchpad>/round6.xml`; stderr `shots/round6/run.log`. Scenario: File > New on the auto-reopened order.xml, two tables placed with the Table tool (Ctrl+T + click; the palette button click itself was not verified, see "Exercised / not reached"), `Table_01` given seven columns of seven different datatypes with a distinct comment each, `Table_02` one column, every Table Editor button/page/popup item pressed, Save As, File > New, Open, reopen the editor.

Result of the data flow: no data loss found. All seven datatypes (`DECIMAL(10,2)`, `TEXT`, `DATE`, `BOOL`, `FLOAT`, `INTEGER`, `VARCHAR(45)`) and all seven comments plus the table comment survive OK + reopen (`13-te-reopen-c.png`), Move Row down and Add Prefix keep the comment on the moved/renamed row (`m3.png`), Save As writes them (`round6.xml`: `idDatatype` 12/28/14/22/7/5/20, `DatatypeParams`, `Comments="c-..."`), and File > New + Open + reopen shows the same grid (`36-reload-c.png` / `m5.png`). No exception, no hang; the only stderr lines are the known #14 GLib-CRITICAL on Return in the grid editor (two lines, `run.log` 07:46:24).

### 15. String-input dialog (index name, column prefix/postfix): the prompt text is hidden behind the edit box

Severity: unreadable (the dialog works, but the user does not see what is being asked; the window title carries the question).
Steps: Table Editor, Indices page, click the "new index" button (top icon right of the index list); or right-click a grid row > "Add Prefix to Column(s)".
Observed: a 391x61 window titled "Please enter the Index Name:" / "Enter a Prefix for the selected columns ..." showing only an edit box with the proposed value (`Table_03_index1284`) and the OK/Cancel icons; of the prompt label only a single "I" is visible at the left edge, the rest is covered by the input panel (`22-both.png`, `28-prefix.png`, `m3.png` middle strip). Typing a name + Return works (index `idx1` created, `23-afterindex-c.png`; prefix `p_` applied, `29-prefixed-c.png`).
Suspected cause: `src/EditorString.pas:101-103` (`SetParams`) places `InputPnl.Left:=PromtLbl.Left+PromtLbl.Width+2` and computes the form width from it before the form is shown; under the LCL the AutoSize label (`src/EditorString.lfm:19-23`, `AutoSize = True`, design caption `Promt:`) has not been re-measured for the new caption at that point, so the panel is placed at the width of the design-time caption and overlaps the real "Name of Index:" / "Prefix:" text. Not confirmed by a probe. Fix scope: small (call `PromtLbl.AdjustSize` / use `Canvas.TextWidth` after `HandleNeeded`, or lay the panel out in `OnShow`).
Status: fixed - cause confirmed: `SetParams` read `PromtLbl.Width` while the LCL AutoSize of the label was still delayed (no handle yet), so the panel was placed for the design caption `Promt:` (32 px) and covered the real prompt; besides, the label sat at `Left = 64`. New `LayoutControls` (called from `SetParams` and again from a `DoShow` override, where the handle exists) calls `PromtLbl.AdjustSize` and takes the larger of the label width and `Canvas.TextWidth(Caption)`, then places the panel and sets `ClientWidth`; the label moved to `Left = 8`. Verified: "Name of Index:" fully readable, edit and buttons follow it (354x61, `fix15/after-index.png`), `idx1` created via Return (`m1.png`); "Prefix:" readable (312x61) and `p_` applied to `d1` -> `p_d1` (`m2.png`, before/after). All `ShowStringEditor` callers pass short prompts (the long text is the title), so none exceeds the screen. No stderr. See notes-to-myself.md "Fix: model-edit #15".

### 16. Datatype palette: dragging a datatype onto a column in the Table Editor grid does nothing

Severity: functional (one of the two datatype-pick paths of the original; typing the datatype in the cell still works).
Steps: open the Table Editor of `Table_02` (column `d1 DOUBLE`); press the mouse on `VARCHAR` in the Datatypes palette ("Common" tab, main window right side), move the pointer in several steps onto the DataType cell of row `d1`, release. Same with `DOUBLE` dropped on the empty new row.
Observed: no change - `d1` stays `DOUBLE` (`34-drag2-c.png`), the empty row stays empty (`m4.png`); no drag cursor was observed, nothing in `run.log`. Round 5 had the same experience once ("one attempt dropped beside the table").
Suspected cause: the drag is started with `CommonDataTypesListView.BeginDrag(False, 5)` in `src/PaletteDatatypes.pas:368` (`OnMouseDown`) and accepted by `src/EditorTable.pas:902-912` (`ColumnGridDragOver` accepts `Source.Name='CommonDataTypesListView'`), i.e. LCL-internal drag between two forms; under GTK2 the LCL drag manager may not deliver `DragOver`/`DragDrop` to a control on another (modal) form, or the mouse-down never reaches the handler because the list view swallows it. Not confirmed - needs a `DebugLn` in `ColumnGridDragOver`. Fix scope: medium (may require `DragMode`/`OnStartDrag` rework or a GTK-DnD replacement). Note `CommonDataTypesListViewDblClick` (`PaletteDatatypes.pas:336`) opens the Datatype *Editor*, it is not a pick path.
Status: worked around (second 45-min time box, `shots/fix16b/`) - the palette drag itself remains non-functional while the Table Editor is open: the editor is `ShowModal` (`src/Main.pas`), so under LCL/GTK2 the palette (floating or docked) receives no mouse events and `ColumnGridDragOver`/`DragDrop` are never reached (see the first attempt above; the drag handlers are left in place for a future non-modal editor). Replacement inside the editor: the column grid popup (right-click a row) now starts with a **Set Datatype** submenu, one submenu per datatype group of the model (`EERModel.DatatypeGroups`, same source as the palette's "All types" tree, empty groups skipped) listing the group's datatypes; the pick goes through the new `ApplyDatatype`, which is the drop handler's assignment code factored out and now shared by `ColumnGridDragDrop`, so a menu pick and a drop are identical: one row (or a parameter-requiring type) -> that row gets the type, empty params, option defaults, and for parameter types the in-place datatype combo opens with the cursor inside `()`; a multi-row selection with a parameter-free type -> every selected non-FK row. `ColPopupMenu.OnPopup` additionally selects the right-clicked row when it lies outside the current selection (previously the popup acted on the last left-clicked row; benefits Delete/Prefix/Postfix too) and disables the submenu on the empty new row / read-only tables. Also noted: the DataType cell already offers a drop-down of all datatype names (double-click or Return on the cell opens `EditorTableFieldDatatypeInplace`'s combo with autocomplete), so two in-editor pick paths exist now. Verified on the real display (`Table_03` of round6.xml): DATETIME on `price` (`after1-c.png`), INTEGER on `active` with UNSIGNED default (`after2-c.png`), BIGINT on rows notes/p_born/active selected with Shift+Down (`m-multi.png`), VARCHAR on `ratio` opening the combo, `20` typed (`after4-c.png`/`after5-c.png`), OK + Ctrl+S: `t.xml` has `idDatatype` 15, 6/6/6, 20 with `DatatypeParams="(20)"`; reopen shows the same grid (`m-final.png`); right-click on `name` without a prior left-click + DATE lands on `name` (`after7-c.png`). Canvas shows column names only in the default display mode, so the type change is visible there only via the reopened editor / saved file. No new stderr lines.

### 17. Table Editor, Advanced page: the "RAID Type" combo is too narrow ("STRIPEC" clipped)

Severity: cosmetic (same class as #12, Row format combo).
Steps: Table Editor > left tree > Advanced.
Observed: the RAID Type combo shows `STRIPEC` with the last letter cut by the dropdown button (`m1.png` bottom strip, `19-advanced-c.png`).
Expected: `STRIPED` readable.
Suspected cause: `src/EditorTable.lfm:395-398` (`RaidTypeLU`, `Width = 90`) - a Delphi-era width; the GTK2 combo needs about 20 px more for the same font. Fix scope: trivial (widen the combo / shift "Number of Chunks" edits).
Status: fixed - cause confirmed (`Width = 90` left about 48 px of text area next to the GTK2 dropdown button; `STRIPED` needs ~55). `RaidTypeLU.Width` 90 -> 106 (ends at 292 inside the 298 px RAID group, level with the `kB` label); the Chunks edits keep 90. Verified `fix17-18/after-adv.png`: `STRIPED` complete, arrow clear of the text. Neighbours checked on the same pass: Row format (`default`, widened in #12), Table Prefix (`Default (no prefix)`), Table Type (`MYISAM  (Standard)`) all complete; Table Type's longer items (`InnoDB  (Transaction-safe ...)`) are clipped in the closed 241 px combo exactly as in the Delphi original (design width, the list shows them in full) - left as is. See notes-to-myself.md "Fix: model-edit #17, #18".

### 18. Table Editor: the page tree (Indices / Table Options / ... / Comments) shows a horizontal and a vertical scrollbar although all five nodes fit

Severity: cosmetic.
Steps: open any Table Editor.
Observed: the tree at the lower left has both scrollbars permanently (`05-te-empty.png`, `m1.png`), the horizontal one obscuring the last 12 px; the other dialogs' list boxes (index list, index columns) have none.
Suspected cause: `src/EditorTable.lfm:535-538` (`PageControlTreeView`, `Height = 155`, width ~115) with the Windows-designed size; LCL-GTK2 tree views add scrollbars when the node width incl. indent + expand button exceeds the client width (`Standard Inserts` node at indent 1 is 96 px). Not confirmed. Fix scope: trivial (`ScrollBars = ssAutoBoth`/`ssNone` or widen the tree).
Status: fixed - cause confirmed, but not the size: the `.lfm` had no `ScrollBars` property, so the LCL default `ssBoth` applied, and `TCustomTreeView.UpdateScrollbars` (lcl/include/treeview.inc) shows both bars *unconditionally* for `ssBoth`/`ssVertical`/`ssHorizontal` - only the `ssAuto*` values hide them when the content fits (Delphi's `ssBoth` behaves like auto). Set `ScrollBars = ssAutoBoth` and an explicit `Width = 124` (was the 121 default; the page control starts at 143). Verified `fix17-18/after-trees.png`: no scrollbar on either page, all five nodes complete. Same pattern applied to every other ported `TTreeView` - fixed in #21.

Observations (not entries): (a) while typing the first column, the first keystrokes went into the Table Name edit (the click into the grid after `windowactivate` was lost - driving artefact), the table became `id`; retyping `Table_01` then produced `Table_03` on exit of the edit (`07-te-7-c.png`). The retype of `Table_03` into its own edit kept the name (`1415.png`), so the self-duplicate branch of `TableNameEdExit` (`src/EditorTable.pas:1304-1340`, `<>EERTable` check) is not the cause; the rename to `Table_03` was not reproduced and is not filed. (b) one comment came back as `c` (first character only) after a click into the Comments cell while the new-row Column Name editor was open (`08-te-cm1-c.png`); the same sequence on rows 6/7 kept the full text (`11-te-cm4-c.png`) - not reproduced, likely the lost-first-click artefact. (c) `Ctrl+A` in the Comments in-place editor selects all (`1415.png`); an earlier `ac-price` came from a lost modifier, not a bug. (d) Escape inside the Column Name editor of the new row discards that row only, the dialog stays open (`m4.png`, by design). (e) Fonts: all Table Editor pages, the confirmation box and the string editor use the same Sans 9-ish face as the other dialogs; nothing bold/blurry/huge was seen; the "Checksum" check box and "kB" label sit at the right edge of their group boxes but are complete.

Proposed grouping: #17 and #18 (and the fixed #12) are the same root cause - Delphi design-time widths in `EditorTable.lfm` too small for GTK2 metrics; one pass over the `.lfm` fixes all. #15 is a separate autosize-before-show issue in `EditorString.pas` that affects every `ShowStringEditor` caller (`grep -n ShowStringEditor src/*.pas`). #16 is on its own (LCL drag-and-drop between forms).

Recommended fix order: #16 (functional, loses a feature), #15 (unreadable, affects every string prompt in the app), #17 + #18 together (cosmetic, one `.lfm` pass).

Exercised / not reached (round 6): exercised - File > New, Table tool via Ctrl+T (two placements), Table Editor open via header double-click (three times), in-place editors for Column Name / DataType / Comments (seven datatypes with params, Tab and Return commits), Table Name edit, Indices page: new index (string editor), delete index (confirmation `25-confirm.png`), delete column from index with empty list (no-op), Table Options / Advanced / Standard Inserts / Comments pages (table comment typed and persisted), grid popup: Move Row down, Add Prefix; OK, Cancel not pressed; Save As via Ctrl+S (GTK file dialog), File > New, Ctrl+O reopen. Not reached - the palette Table button by mouse (only Ctrl+T used), the datatype *dropdown* in the DataType cell (the cell only offered the text editor on click/Tab; no combo appeared), Delete Row(s) / Add Postfix / Add Column(s) to Selected Index from the popup, Move Row up, the Index Type combo change, the RAID Type dropdown, Table Type / Table Prefix combos, the Weak-entity / n:m check boxes, the Standard Inserts popup, canvas display of datatypes (the default table display shows names only, `12-canvas-c.png`), SQL export of round6.xml.

### Verification pass (2026-09-09, HEAD b9cfb2f)

30-minute time box on the real display, binary rebuilt with `lazbuild --build-all` from b9cfb2f. Screenshots `<scratchpad>/shots/verify6/`, saved model `shots/verify6/verify6.xml`, stderr `shots/verify6/run.log` (20 lines of the known #14 GLib-CRITICAL on Return in the grid, nothing else, no exception, no hang).

| Entry | Result | Evidence |
|---|---|---|
| #15 string dialog (index name) | verified - "Name of Index:" fully readable, 354x61, `Table_02_idx1` created from the returned value | `27-indexdlg.png`, `m8.png`, `28-index-c.png` |
| #15 string dialog (Add Prefix) | verified - "Prefix:" readable, 312x61, `p_` applied (`name` -> `p_name`) | `39-prefixdlg.png`, `m11.png` |
| #16 Set Datatype submenu | verified - popup starts with "Set Datatype", 7 groups, single row DECIMAL, multi-row (Shift+Down) TIMESTAMP on two rows, LONGTEXT from "Blob and Text Types"; all persisted | `m1.png`, `14-groups.png`, `15-numeric.png`, `m3.png`, `m4.png`, `25-blobmenu.png`, `26-t2-c.png` |
| #17 RAID Type combo | verified - `STRIPED` complete, arrow clear of the text | `38-advanced-c.png`, `m10.png` |
| #18 page tree scrollbars | verified - no scrollbar on the Indices, Advanced and Comments pages, five nodes complete | `05-te.png`, `28-index-c.png`, `m10.png` |

Scenario result (task 1): File > New (`Noname2`), `Table_01` placed with Ctrl+T + click, `Table_02` with the palette Table button + click (both work, `04-tables-c.png`). `Table_01` (renamed to `Table_03` by the app, see #19) received six columns through all three pick paths - INTEGER (default), DATE picked from the in-place drop-down list (double-click on the DataType cell, `10-te-combo-c.png`, `11-dtlist-c.png`), DECIMAL via Set Datatype then `(10,2)` typed into the combo, TIMESTAMP via multi-row Set Datatype, TEXT typed into the combo opened with Return, VARCHAR(45) typed after Tab from the Column Name cell (`m6.png`) - each with a distinct comment. OK, Save As (`verify6.xml`: `idDatatype` 5/14/12/16/28/20, `DatatypeParams` `(10,2)` and `(45)`, `Comments` c-id/c-name/c-price/c-flag/c-notes/c-extra), File > New, Open, reopen: the grid shows every datatype and comment (`37-reopen-c.png`). `Table_02` got three columns (`26-t2-c.png`) and the index, but a stray Escape sent by the driver with no in-place editor open cancelled its editor (Escape = Cancel, by design), so `Table_02` is saved without columns - driving artefact, not a bug. Note for future drivers: letters typed on a selected DataType cell are ignored by design (`ColumnGridKeyDown` opens the text editor only for Column Name / Default / Comments); the DataType cell edits only through the combo (Return, double-click, or Tab from the name).

Tree-view sweep (task 4): `.lfm` `TTreeView`s without a `ScrollBars` line (LCL default `ssBoth` = bars always shown, see #18): `src/PaletteModel.lfm` `ModelTV` - reachable (DB Model palette, main window) - observed: both scrollbars permanently with a single node (`sweep-modeltv-new.png`) and with the 13 `order.xml` tables (`sweep-modeltv.png`); `src/EditorQuery.lfm` `StoredSQLTreeView` - reachable (Query mode, lower left) - observed: permanent vertical bar with four nodes (`sweep-storedsql.png`); `src/PaletteDatatypes.lfm` `AllDataTypesTV` - reachable ("All types" tab) - not checked (time); `src/PaletteModel.lfm` `TablesTreeView`, `src/Options.lfm` and `src/OptionsModel.lfm` `PageControlTreeView`, `src/DBConnSelect.lfm` `DBConnTV`, `src/EERStoreInDatabase.lfm` `SavedModelsTV`, `src/EERPlaceModel.lfm` `LMTreeView` - not reached. `src/EditorTable.lfm` `PageControlTreeView` is the only one with `ScrollBars = ssAutoBoth`.

Regressions (task 5): none seen on the Table Editor - Indices (new index, Index Type combo shows INDEX), Advanced, Comments pages, the column popup (all seven items present), OK button, in-place editors, the string dialogs; Table Options / Standard Inserts and the Cancel button were not exercised on purpose.

### 19. Table Editor: renaming a table to its own original name yields the next free number (Table_01 -> Table_03)

Severity: functional (silent rename; a table cannot get its original name back inside one editor session).
Steps: open the Table Editor of `Table_01` (a `Table_02` exists), change Table Name to `id`, Tab (leaves the edit), change it back to `Table_01`, Tab.
Observed: the edit shows `Table_03` (`07-te-c.png`); repeating the rename keeps `Table_03` (`08-te-c.png`); the saved model has `Tablename="Table_03"`. Same effect as round 6 observation (a), now reproduced deliberately.
Cause (confirmed by reading): the editor works on a copy - `SetTable` creates `EERTable` with `TEERTable.Create(self, ...)` and `EERTable.Assign(SourceEERTable)` (`src/EditorTable.pas:397-407`); `TableNameEdExit` (`src/EditorTable.pas:1401-1436`) walks `EERModel.Components` and skips only `<>EERTable`, i.e. the copy, so the model's own `SourceEERTable` (still `Table_01` until OK) counts as "another table" once the edit text was changed away and back, and the last digit is bumped past `Table_02` to `Table_03`. Fix scope: trivial (also skip `SourceEERTable` in the comparison).
Status: fixed - `TableNameEdExit` now skips both the working copy and `SourceEERTable` (see notes-to-myself "Fix: model-edit #19"). Verified on the real display: `Table_03` -> `foo` -> `Table_03` stays `Table_03` (Return and Tab), `Table_01` -> `foo` -> `Table_01` + OK gives `Table_01` on the canvas and in the DB Model tree (`shots/fix19/1013-c.png`); a real clash (`Table_02` typed while `Table_02` exists) is still bumped to the next free number (`0507-c.png`).

### 20. Table Editor: a comment typed into a cell while the new-row Column Name editor is open keeps only its first character

Severity: functional (silent data loss of the comment text).
Steps: commit a column name or a datatype on the last row with Return so the empty new row opens its Column Name editor (cursor visible in the diamond row); click the Comments cell of an existing row; type `c-extra`, Return.
Observed: the cell reads `c` (`m1.png` row `name`, `m7.png` top row `extra`); repeated 2/2 under this condition, plus round 6 observation (b). When no new-row editor is open the same sequence keeps the full text (`m7.png` bottom, `c-name` retyped; `c-flag`, `c-notes`, `c-price`).
Suspected cause: the first keystroke reaches the grid (`ColumnGridKeyDown` -> `EditCellStr(NewName)` with the one character) while the still-open `EditorTableFieldEdit` of the new row is closed/re-parented by the click; the remaining keystrokes arrive before the new editor has focus and are dropped. Not confirmed by a probe. Fix scope: small (close the pending editor in `ColumnGridMouseUp`/`SelectCell` before starting the next, or give the new editor focus synchronously).
Status: fixed - stuck mouse capture, not a focus race. Probes (`gtk_grab_get_current`, `GetCaptureControl`) showed that after the click the grid kept the LCL mouse capture (GTK grab on the grid widget) for good: the click's button press first moved the focus off the open editor, whose `OnExit` -> `ApplyChanges` -> `HideEdit` ran `Application.ProcessMessages` (a CLX-era workaround) *inside* the grid's button-press handling, so the button *release* was processed before the LCL took the capture for the press and nothing released it afterwards. Every following key press was then emitted on the grabbed grid widget; the LCL redirected the LM_KEYDOWN to the focused editor (its `OnKeyDown` fired, hence the "no emission of signal key-press-event to stop" GLib-CRITICAL on each key) but the GtkEntry itself never received the key, so only the first character (inserted through `ColumnGridKeyDown` -> `EditCellStr('c')`) survived; Return still worked because it is handled in `OnKeyDown`. Fix: `HideEdit` no longer pumps the message queue (`src/EditorTableField.pas`). Verified on the real display (`shots/fix20/`): the exact repro (`zz1` + `VARCHAR(7)` typed + Return -> new-row editor open, click `name` Comments, `c-extra` + Return) keeps `c-extra` (`a12.png`), Tab from an existing row's name through DataType and Default Value into Comments takes `via-tab` (`b012.png`), Column Name / DataType typing unchanged (no doubling, no loss), OK + Ctrl+S + reopen shows all of it (`c1-reopened-c.png`, `fix20.xml`), and the GLib-CRITICAL lines of #14 did not appear once in the run.

### 21. Permanent scrollbars on all ported tree views

Severity: cosmetic.
Steps: open any `TTreeView` of the port whose content fits its box - e.g. the DB Model palette with a one-node model, or Query mode's stored-SQL tree (four nodes).
Observed (verification pass of 2026-09-09, `verify6/sweep-modeltv-new.png`, `sweep-modeltv.png`, `sweep-storedsql.png`): both scrollbars (ModelTV) or a vertical bar (StoredSQLTreeView) shown permanently although nothing overflows.
Cause: the same as #18 - none of the ported `.lfm` tree views carried a `ScrollBars` line, so the LCL default `ssBoth` applied and `TCustomTreeView.UpdateScrollbars` shows the non-auto bars unconditionally; Delphi's `ssBoth` was effectively automatic.
Status: fixed - `ScrollBars = ssAutoBoth` added to every `TTreeView` block that lacked it (`EditorTable.lfm` already had it since #18): `src/PaletteModel.lfm` (`ModelTV`, `TablesTreeView`), `src/PaletteDatatypes.lfm` (`AllDataTypesTV`), `src/EditorQuery.lfm` (`StoredSQLTreeView`), `src/Options.lfm` and `src/OptionsModel.lfm` (`PageControlTreeView`), `src/DBConnSelect.lfm` (`DBConnTV`), `src/EERStoreInDatabase.lfm` (`SavedModelsTV`), `src/EERPlaceModel.lfm` (`LMTreeView`), `Plugins/SimpleWebFront/Main.lfm` (`PageControlTreeView`); main project and the SimpleWebFront plugin rebuilt. Verified on the real display with `bin/Examples/order.xml` (`shots/fix-trees/`): DB Model tree - no bars with the 13 collapsed tables (`modeltv-fits.png`), vertical bar once six tables are expanded past the bottom (`modeltv-over.png`, `modeltv-bottom.png`); Datatypes "All types" - no bars with the four collapsed groups, vertical bar only once expanded (`datatypes-all.png`, `datatypes-over.png`); stored-SQL tree in Query mode - four nodes, no bars (`storedsql.png`); Select Database Connection tree - nine nodes, no bars (`dbconn.png`); DBDesigner Options and Model Options page trees - four nodes each, no bars (`options-tree.png`, `moptions-tree.png`). Not reached (fixed by the same line, unverified): `TablesTreeView`, `SavedModelsTV` (needs a live DB connection), `LMTreeView` (Place Model from library), the SimpleWebFront page tree. No new stderr lines. See notes-to-myself.md "Fix: model-edit #21".

Observations (verification pass, not entries): (a) after File > New the DB Model palette still listed the 13 tables of the previous `order.xml` model until the first table was placed (`02-design-half.png` vs `sweep-modeltv-new.png`); seen once, not re-checked. (b) Set Datatype > DECIMAL does not open the combo with `()` (DECIMAL's `ParamRequired` is false; the params were typed via Return on the cell afterwards) - by design, same as the palette drop in the original. (c) Escape with no in-place editor open cancels the whole Table Editor without a confirmation - by design, but it cost the `Table_02` columns in this pass; a "discard changes?" prompt would be a usability improvement, not filed. (d) The GTK Save dialog's "folder could not be displayed" error box is unnamed and opens behind the file dialog, which then ignores all input until the box is found and dismissed (`36-hidden.png`) - GTK behaviour triggered by a mistyped path, not an app bug.

## Round 7 (2026-09-09): grid popup, editor combos, relations, SQL export

Date: 2026-09-09 (30-minute time box, real display DISPLAY=:0, binary rebuilt with `lazbuild --build-all` from 646e2dd). Screenshots `<scratchpad>/shots/round7/`, working model `shots/round7/round7.xml` (copy of round6.xml: `Table_03` with seven typed+commented columns, `Table_02` with `d1`), exports `shots/round7/round7.sql` (SQLite target) and `round7-mysql.sql` (My SQL target), stderr `shots/round7/run.log` + `run2.log` (only the canberra-gtk-module line each; no exception, no GLib-CRITICAL, no hang). Two app sessions (the first was stopped early by mistake after the relation delete; the second reopened the saved round7.xml).

Result of the data flow: no data loss found. Every grid popup item, the Indices page, the Table Type / Index Type / Relation Kind / Target Data Base dropdowns, three relation kinds plus the n:m join table, the Relation Editor round trip, Ctrl+Del and both SQL exports behaved; details in "Exercised / not reached". Two low-severity entries follow.

### 22. Table Editor column grid: Shift+click does not extend the row selection (only Shift+Down does)

Severity: functional (minor - multi-row Delete / Set Datatype / Prefix / Postfix are reachable only via the keyboard range).
Steps: Table Editor of `Table_03`; click the `notes` row; Shift+click the `p_born` row; right-click > Delete Row(s).
Observed: only `p_born` is highlighted after the Shift+click and only `p_born` is deleted (`m2.png` top/bottom, `08-te-multisel-c.png`, `09-te-del2-c.png`). The same with click on `notes` + Shift+Down highlights `notes`+`ratio_x` and Delete Row(s) removes both (`m3.png`, `10-te-shiftdown-c.png`, `11-te-del3-c.png`), so the popup action itself is fine.
Expected: Shift+click extends the range as with `goRangeSelect` in the Delphi original.
Suspected cause: `src/EditorTable.pas:1347-1395` (`ColumnGridMouseDown`) skips its own `ColumnGrid.Selection:=` when `ssShift` is held and relies on the grid's built-in range extension, but it also removes `goRangeSelect` from `ColumnGrid.Options` inside the same mouse-down (line 1392; re-added in `ColumnGridMouseUp`, line 1458). In the LCL `TCustomGrid.MouseDown` fires `OnMouseDown` (via `inherited`) *before* it evaluates the Shift range, so by the time the grid would extend the selection the option is gone; Delphi's `TCustomGrid.MouseDown` evaluates the range first. Not confirmed by a probe. Fix scope: small (keep `goRangeSelect` while `ssShift` is in `Shift`, or extend the selection explicitly from `DragStartRow`).
Status: fixed - cause confirmed by reading `lcl/grids.pas` (`TCustomGrid.MouseDown` calls `inherited` = `OnMouseDown` first, then `SelectActive:=(goRangeSelect in Options)`; the Delphi original had the same handler code). `ColumnGridMouseDown` keeps `goRangeSelect` when `ssShift` is held; second finding on the way: the LCL leaves the protected `SelectActive` on after a Shift range, so the `ColumnGrid.Row:=ARow` of the #16 popup handler re-extended the range from the pivot (right-click on `name` outside `ratio..id` deleted all three). `ColumnGridMouseUp` and `ColPopupMenuPopup` now reset it through a `TColumnGridAccess = class(TDrawGrid)` cracker, and the popup sets `Row` before `Selection`. Verified on the real display (`shots/fix22/`): click `notes` + Shift+click `active` highlights three rows (`v2-shiftclick-c.png`), Set Datatype BIGINT hits exactly those three (`v3-setdt-c.png`), Delete Row(s) on a fresh three-row range removes all three (`w1-range-c.png`/`w2-del-c.png`), a plain click collapses the range (`w3-collapse-c.png`), #16 right-click outside the range deletes only that row (`w4-del16-c.png`), #20 typing after a click works (`w5-type-c.png`). Not a regression: Set Datatype collapses the selection to the target cell afterwards (`ColumnGrid.Col:=3; Row:=ARow` at the end of `ApplyDatatype`, original code). See notes-to-myself.md "Fix: model-edit #22".

### 23. SQL export writes an index that has no columns as `FULLTEXT INDEX idx7()` (invalid SQL; FULLTEXT also emitted for the SQLite target)

Severity: functional (minor - only when the user leaves an index without columns; the original DBDesigner 4 most likely did the same, so not a port regression).
Steps: Indices page: new index `idx7`, Index Type FULLTEXT INDEX, add `name` via the grid popup "Add Column(s) to Selected Index", remove it again with the eraser (`m5.png`); OK; File > Export > SQL Create Script, Target Data Base SQLite, Save Script to file; repeat with My SQL.
Observed: `round7.sql` contains `CREATE FULLTEXT INDEX idx7 ON Table_03 ();` and `round7-mysql.sql` contains `FULLTEXT INDEX idx7())` inside the CREATE TABLE - both are syntax errors, and SQLite has no FULLTEXT indexes at all. Everything else in both files is right: both/all three tables, all datatypes with parameters (`DECIMAL(10,2)`, `VARCHAR(45)`, `INTEGER UNSIGNED`), the PRIMARY KEYs, the FK columns, `FOREIGN KEY ... REFERENCES` with the ON DELETE/UPDATE actions, the FK indexes, `ENGINE=InnoDB` (Table Type picked from the combo) and, for My SQL, the table comment and every column comment (`COMMENT 'c-id'` etc.). The SQLite file has no column comments because `GetSQLColumnCreateDefCode` emits `COMMENT` only for `DatabaseType = 'My SQL'` (`src/EERModel.pas:9601-9607`) - by design.
Suspected cause (confirmed by reading): `TEERTable.GetSQLCreateCode` (`src/EERModel.pas:9225-9260`) appends the index kind and the `(`...`)` list without checking `TEERIndex(Indices[i]).Columns.Count>0`, and the `ik_FULLTEXT_INDEX` branch (line 9243) is not translated for non-MySQL targets. Fix scope: small (skip empty indexes, or degrade FULLTEXT to INDEX when `DatabaseType<>'My SQL'`).
Status: fixed - `GetSQLCreateCode` skips an index with `Columns.Count=0` (inline list and portable `CREATE INDEX` alike; there is no DROP INDEX counterpart in the exporter) and writes `ik_FULLTEXT_INDEX` as a plain `INDEX` when `DatabaseType = 'SQLite'` (other targets keep `FULLTEXT INDEX`; PRIMARY KEY handling untouched). Verified on the real display with `shots/fix23/fix23.xml` (= round7.xml, `idx7` without columns): My SQL export `after-mysql.sql` has no `idx7` line (diff to `round7-mysql.sql` is exactly the dropped `FULLTEXT INDEX idx7())`), SQLite export `after-sqlite.sql` has no `idx7` line and loads in sqlite3; with `fix23b.xml` (`idx7` on `name`) the SQLite export writes `CREATE INDEX idx7 ON Table_03 (name);` and loads in sqlite3 (`after-b-sqlite.sql`). Not re-exported: the My SQL script of `fix23b.xml` (unchanged code path, `FULLTEXT INDEX idx7(name)` as before). See notes-to-myself.md "Fix: model-edit #23".

Observations (not entries): (a) the relation label is hidden for the "1:1 (Descendent Obj.)" kind (`rk_11Sub`) - `src/EERModel.pas:12181-12191` hides `RelCaption` for that kind on purpose and `PaintObj2Canvas_RelMiddle` draws no icon for it (`30-rel11-c.png` shows the line and the FK columns but no label; the double-click on the line's middle then opens nothing because there is no caption control). Left-strip button order in the main window (top to bottom, crop y 315/337/360 | 391/413/436 in `01-main-c.png`): 1:n Sub tool (creates `Kind=2`, "1:n (Non-Identifying)"), 1:1 Non-Id, n:m, 1:1 Sub (`Kind=4`), 1:1 (`Kind=0`), 1:n - it follows the design-time `Top` values of `src/Main.lfm` (`wtRel1nSubSBtn` 240 ... `wtRel1nSBtn` 360), not the two-column layout of `PaletteTools.lfm`. (b) A selected relation is drawn dotted and its label frame dotted (`26-canvas-rel-c.png`) - `src/EERModel.pas:11121-11124` and `11266` (`selected` -> `psDot`), by design. (c) Right after the 1:1 relation was placed across the n:m join table, the join table was painted with only three of its five FK rows (`38-rel11-c2.png` in `m8.png`); the next repaint (`40-canvas-final-c.png`) and the saved XML have all five (`Table_03_id`, `Table_03_price`, `Table_02_Table_03_price`, `Table_02_Table_03_id`, `Table_02_d1`, all PK+FK) - transient, seen once, not filed. (d) The n:m tool places `Table_03_has_Table_02` midway between the two tables where it overlaps `Table_02`, and the short `Rel_03` label is then hidden under the `Table_02` header (`37-relnm-c.png`) - placement logic of the original, not filed. (e) The "Weak entity" group of the Table Editor holds only the "is n:m Table" check box (`src/EditorTable.lfm:650-660`, `nmTableCBox`); ticking it is saved as `nmTable="1"` and the table is drawn with a dashed frame while selected only. (f) The Relation Kind combo shows `1:n (Non-Identifying)` with the `)` touching the dropdown button (`23-releditor.png`) - readable, not filed. (g) The Export SQL Script dialog (`35-sqlexport.png`) and the Place / Link Model dialog (`43-placemodel.png`, model preview with both scrollbars because the 4096x2842 canvas really overflows) show no clipped or missing controls; the Place dialog is reached by File > Add/Link Model > from File **and then a click on the canvas** (the menu item only sets `wtPlacementFromFile`, `src/Main.pas:3761-3764`; the canvas click posts `QEventType_PlaceModelFromFile`, `src/EERModel.pas:1651-1654`). Its `LMTreeView` (#21, unverified) is not shown on this path. (h) Driving: GTK submenu items need a hover (pointer motion inside the item, ~0.5 s) before the click, otherwise the click closes the menu without firing; the first click after `windowactivate` on a modal dialog is regularly lost (Relation Editor Cancel needed two attempts).

Proposed grouping: #22 stands alone (LCL vs Delphi order of `TCustomGrid.MouseDown` vs `OnMouseDown`, `EditorTable.pas`). #23 stands alone (SQL generator, `EERModel.pas` `GetSQLCreateCode`), and is most likely inherited from the original. Nothing in this round shares a cause with rounds 5/6 (#15-#21 all verified working on the way: string dialogs readable, Set Datatype submenu present, RAID/Row format combos complete, page tree without bars, table rename kept, comments kept).

Recommended fix order: #23 (exported script fails on the server), then #22 (usability).

Exercised / not reached (round 7): exercised - Table Editor via header double-click; grid popup: Set Datatype (present, not re-tested), Add Postfix (`_x` on `ratio`, comment kept, `m1.png`), Add Column(s) to Selected Index (`id` into PRIMARY, key icon and NN set, `06-te-addidx-c.png`; `name` into `idx7`), Delete Row(s) single (`active`, no confirmation, `07-te-del1-c.png`) and range (`notes`+`ratio_x` via Shift+Down), Move Row up/down not repeated (round 6); Indices page: new index (string dialog readable), Index Type dropdown with all four kinds (`m4.png`), FULLTEXT picked, remove index column with the eraser; Table Type dropdown with all six engines (`18-tabletype-dd.png`, InnoDB picked and exported as `ENGINE=InnoDB`); Table Options page (Delay / Pack Keys / Checksum boxes and Row format visible, not toggled); Advanced page (RAID Type shows `STRIPED`, not dropped down); "is n:m Table" box (ticked, persisted); OK; canvas after OK (`m6.png`); relations: 1:n non-identifying via the first strip button (`Rel_01`, line, label, two FK columns, `22-rel1-c.png`), Relation Editor via label double-click (`23-releditor.png`, all fields), rename to `RelA` + kind 1:n identifying via the dropdown (`24-kind-dd.png`, six kinds), OK, reopen (`m7.png`), Cancel, Ctrl+Del + Confirmation (`28-delconfirm.png`) - relation and both FK columns removed from canvas and XML (`29-canvas-deleted-c.png`); 1:1 Sub (`Kind=4`), n:m (join table + `Rel_02`/`Rel_03`, `37-relnm-c.png`), 1:1 (`Rel_04`, editor `39-relnm-editor.png`); Ctrl+S three times, XML checked by script; File > Export > SQL Create Script for SQLite and My SQL (Target Data Base dropdown `41-target-dd.png`, six targets), Save Script to file via the GTK dialog; File > Add/Link Model from File + canvas click + Open dialog + Place / Link Model dialog (Escape did not close it; the app was stopped with it open, its File menu not tried). Not reached - the Reference Definition combo and its On Delete/Update lists in the Relation Editor, the Visibility combo, the FK grid Comment cell, the Table Prefix combo dropdown, Pack Keys / Row format / RAID Type dropdowns (closed text checked only), the Standard Inserts page, Cancel of the Table Editor, Copy Script to Clipboard, the Export dialog's option check boxes, the Place dialog's Selection/Place menus and `LMTreeView`, the SQL Drop / Optimize / Repair scripts, undo after the relation delete, a Relation Editor open on the n:m relations (`Rel_02`'s double-click landed on `Rel_04`'s crossing line).

## Round 8 (2026-09-09): round trip of a tool-created model

Date: 2026-09-09 (30-minute time box, real display DISPLAY=:0, binary rebuilt with `lazbuild --build-all` from df0c125). Screenshots and files: `<scratchpad>/shots/round8/` (`$R` below); model `<scratchpad>/round8.xml`, modified model `<scratchpad>/round8-mod.xml`; stderr `$R/run1.log`..`run3.log` (three app sessions; each holds only the canberra-gtk-module line: no exception, no GLib-CRITICAL). MySQL database `dbdround8` (created and dropped by this round), SQLite file `$R/round8.sqlite`.

Model: because of the time box the model was **not** built in the Table Editor this time; `round8.xml` was derived by script from the tool-created `round6.xml` of round 6 (same datatype/comment data written by the tool) and extended to the requested shape: table `parent` (comment "table comment parent") with `id INTEGER PK NN AI`, `name VARCHAR(45) NN`, `price DECIMAL(10,2)`, `p_born DATE`, `notes TEXT`, `active BOOL`, `ratio FLOAT`, `created DATETIME`, `big BIGINT UNSIGNED`, each with a comment `c-...`, UNIQUE index `parent_name_uq(name)`; table `child` (comment "table comment child") with `cid INTEGER PK NN AI`, FK column `parent_id INTEGER NN` (comment `c-fk`), `label VARCHAR(45)`; 1:n non-identifying relation `parent_child` (`Kind=2`, RefDef ON DELETE/UPDATE CASCADE, FK index `child_FKIndex1`). The tool loads it and draws both tables and the relation (`01-main-c.png`, `03-main-c.png`). Index and relation creation in the tool itself were exercised in round 7.

### Round-trip status

| Stage | MySQL 8 (`dbdround8`) | SQLite (`round8.sqlite`) |
|---|---|---|
| A export (File > Export > SQL Create Script) | **works** - `$R/round8-mysql.sql`: every column with its exact type and parameters (`DECIMAL(10,2)`, `VARCHAR(45)`, `BIGINT UNSIGNED`, `BOOL`, `DATETIME`, ...), `NOT NULL`, `AUTO_INCREMENT`, `COMMENT 'c-...'` on every column, `COMMENT = '...'` on both tables, `UNIQUE INDEX parent_name_uq(name)`, `INDEX child_FKIndex1`, `FOREIGN KEY ... ON DELETE CASCADE ON UPDATE CASCADE`. Cosmetic: 12 blank lines between the two tables | **works** - `$R/round8-sqlite.sql`: `INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT`, all types with parameters, `CREATE UNIQUE INDEX`, `CREATE INDEX`, FK with actions. By design (see #23): no column/table comments (only in the header comment lines) and no `UNSIGNED` (`src/EERModel.pas:9541-9549` emits datatype options only for `DatabaseType = 'My SQL'`) |
| B load (roundtrip helpers) | **works** - `$R/mysql-roundtrip.txt`: zero errors, 2 tables, all 12 columns with types/NOT NULL/auto_increment, FK `child_ibfk_1` CASCADE/CASCADE, `parent_name_uq` unique, `child_FKIndex1`; column and table comments in `information_schema` | **works** - `$R/sqlite-roundtrip.txt`: zero errors, 2 tables, all columns, FK CASCADE/CASCADE, unique index, AUTOINCREMENT on both tables |
| C connect + Reverse Engineering | **works** - connection selector (`15-connsel-c.png`, `20-connsel-c.png`), Reverse Engineering dialog (`17-revdlg-c.png`, both tables listed), Execute adds both tables and the relation (`21-main-rev-c.png`); one "Access denied" error box on the way was caused by the retained password, see observation (a) | **works** - same path with the SQLite connection (`25-revdlg-sqlite-c.png`, `26-main-rev-sqlite-c.png`) |
| D compare (`compare_models.py`) | **data loss** - `$R/compare-mysql.txt` / `reveng-mysql.xml`: tables, all columns by type/params/PK/NN/AutoInc, both indexes (`parent_name_uq` as UNIQUE), the relation with FK mapping, Kind and CASCADE rules all OK; `big` keeps UNSIGNED; `BOOL` comes back as `TINYINT(1)` (MySQL's own mapping, not a bug). **All 12 column comments and both table comments are lost** (#24) | **works** (as far as SQLite can) - `$R/compare-sqlite.txt` / `reveng-sqlite.xml`: all columns, both indexes, the relation with CASCADE rules OK; comments and UNSIGNED were not in the script (stage A) so they cannot come back |
| E synchronisation after changing `price` to `DECIMAL(12,4)` / comment `c-price-mod` (`round8-mod.xml`) | **works** - dialog `22-sync-c.png` ("2 Table(s) in Database, 2 Table(s) in Model"), progress `23-sync-done-c.png`: "Modifying column price from table parent ... 12 Columns compared. 1 Column modified."; `information_schema` afterwards: `price decimal(12,4)` comment `c-price-mod`, all other comments and both table comments kept | **fails, and the dialog can no longer be closed** (#25; cause of the failure = sqlite-bug-catalog #8) |
| F Store Model in Database / Place Model | not reached | not reached |

### 24. MySQL reverse engineering drops every column comment and both table comments
Status: fixed - `EERMySQLReverseEngineer` (`src/DBEERDM.pas`) now runs `show full fields` and assigns the `Comment` column to `TEERColumn.Comments`, reads the table comment from `show table status like '<table>'` into `TEERTable.Comments` (old servers' `; InnoDB free: ...` suffix stripped by `StripMySQLTableStatusComment`; the query is wrapped in try/except and both reads check `FindField('Comment')`, so a driver without the column is harmless), and the FK relations built from `information_schema` carry the FK column's comment in `FKFieldsComments`. SQLite path (`EERSQLiteReverseEngineer`) untouched. Verified on the real display (`shots/fix24/`): `dbdround8` built from `round8-mysql.sql`, File > Database > Reverse Engineering > `Round8MySQL` > Execute, Save As `reveng.xml`: all 12 column comments (`c-id` ... `c-label`), `table comment parent` / `table comment child` and `FKFieldsComments="c-fk"` present; Table Editor of `parent` shows the comments in the grid (`14-tableeditor.png`) and the Comments page (`15-tableeditor-comments.png`). Regression: reverse engineering the SQLite `round8.sqlite` lists both tables (`17-revdlg-sqlite.png`), Execute ends with the normal "2 table(s) already exist ... skipped" box, no exception in `run.log`. Follow-up (second commit): the `Engine` column of the same `show table status` row is now mapped onto `TEERTable.TableType` (`MySQLEngineToTableType`: MyISAM 0, InnoDB 1, MEMORY/HEAP 2, BDB 3, ISAM 4, MERGE/MRG_MYISAM 5, anything else keeps the model default) - the inverse of the `ENGINE=` mapping in `GetSQLCreateCode`. Verified (`shots/fix24b/`): `dbdround8b` from `fix24b-mysql.sql` (`parent`/`child` ENGINE=InnoDB, new `logtab` ENGINE=MyISAM), reverse engineered and saved as `reveng.xml`: `TableType="1"` for parent and child, `"0"` for logtab; Table Editor of `parent` shows `InnoDB (Transaction-safe ...)` in the Table Type combo (`15-tableeditor.png`); File > Export > SQL Create Script target My SQL (`reveng-mysql.sql`): `ENGINE=InnoDB` on parent and child, no clause on logtab. Still open: the export writes no clause for type 0, so a MyISAM table lands as InnoDB on a MySQL >= 5.5 server (its default engine) - pre-existing export behaviour, not a reverse-engineering issue. See notes-to-myself.md "Fix: model-edit #24".

Severity: functional (data loss on the round trip; the comments are in the database - stage B - and synchronisation reads them, so only this path loses them).
Backend: MySQL 8 (SQLite has no comments to lose).
Steps: load `$R/round8-mysql.sql` into `dbdround8`; File > New; Database > Reverse Engineering; connection `Round8MySQL`; Execute; File > Save As `$R/reveng-mysql.xml`.
Observed: every `<COLUMN>` has `Comments=""` and both `<TABLE>` elements have `Comments=""` (`grep Comments= $R/reveng-mysql.xml`), while `SELECT COLUMN_NAME, COLUMN_COMMENT FROM information_schema.COLUMNS ... 'parent'` returns `c-id` ... `c-big` and `TABLE_COMMENT` returns the two table comments. Everything else is recovered (see stage D). The relation's `FKFieldsComments` is also empty (was `c-fk`); the relation comment "relation comment" is not stored in the database at all.
Expected: column comments from `SHOW FULL FIELDS` (column `Comment`) and table comments from `SHOW TABLE STATUS` (`Comment`), as the DBDesigner 4 model can hold them.
Suspected cause (confirmed by reading): `TDMDBEER.EERMySQLReverseEngineer` (`src/DBEERDM.pas:429-...`) lists the columns with `show fields from <table>` (line 499, columns Field/Type/Null/Key/Default/Extra - no Comment) and never assigns `TEERColumn.Comments` or `TEERTable.Comments` (`grep -n "Comments" src/DBEERDM.pas` finds no assignment in that routine). The synchronisation path already uses `show full fields` (line 2393-2397) and reads the comment, so the reverse engineer only needs the same query plus `show table status`. Same family as mysql-bug-catalog #9 (sync used to lose comments, fixed) - the reverse direction was never done. Most likely inherited from the original (DBDesigner 4 used `show fields` too), so not a port regression. Fix scope: small.

### 25. SQLite synchronisation stops silently after "Get Tables from DB" and the dialog's Close button stops working
Status: fixed (message/close; SQLite sync itself stays a limitation, sqlite #8) - stuck-dialog cause confirmed with file probes: the `near "show": syntax error` escaped `EERMySQLSyncDB` and `SubmitBtnClick` into `TMainForm.AppException`, whose dialog opened *behind* the modal sync form (window `492x134` present in `xwininfo`, GTK's modal grab stays with the sync form), so nothing was visible, Close/Execute went to the sync form whose own `ShowModal` loop was suspended by the second modal loop, and Escape closed the hidden exception dialog. Fix: `SubmitBtnClick` catches every exception of the sync, logs `ERROR: <msg>` / `Synchronisation aborted.` into the progress memo and shows one `MessageDlg` on top; `AppException`'s dialog gets `PopupMode:=pmAuto` so it is stacked above the active modal form should another exception escape; Escape now closes the dialog (`FormKeyDown`, it was never wired - only F1). SQLite path: `EERMySQLSyncDB` skips `SET FOREIGN_KEY_CHECKS`, lists the tables from `sqlite_master` like the reverse engineering, logs them, then stops with a clear message *before* the first MySQL-only step ("Create non existing table" / `show full fields` / `ALTER TABLE ... MODIFY`) - that is exactly where it stops now; `GetDBTables` no longer counts `sqlite_sequence` (header now "2 Table(s) in Database"). Verified on the real display (`shots/fix25/`): SQLite Execute -> error box `10-sqlite-errbox-c.png` on top, OK, Close closes; second run OK + Escape closes; database untouched. MySQL: `round8-mod.xml` against a `dbdtest` built from `round8-mysql.sql` -> "Modifying column price from table parent ... 1 Column modified." (`21-mysql-sync-done-c.png`), `information_schema`: `decimal(12,4)` / `c-price-mod`, Close closes. Before: `02-sync-stuck-c.png`. See notes-to-myself.md "Fix: model-edit #25".

Severity: functional (the sync itself is known to be MySQL-only, sqlite-bug-catalog #8; new in this round: no error message at all, and the modal dialog cannot be closed - Close, Escape and Alt+F4 do nothing, so the application has to be killed).
Backend: SQLite.
Steps: with the reverse-engineered SQLite model open (or `round8.xml`), Database > Database Synchronisation; connection `Round8SQLite`; Connect; Execute; then click Close.
Observed: the progress memo shows "3 Table(s) in Database, 2 Table(s) in Model" (the header counts `sqlite_sequence`; compare mysql-bug-catalog #13), then "Synchronisation started. / Get Tables from DB" and nothing more (`28-sync-sqlite-done-c.png`); no error box and no exception dialog (`xwininfo -root -tree` lists no further window; `run3.log` is empty); the database is untouched (`sqlite3 .tables`). The dialog still reacts: Execute a second time appends the same two lines again (`31-sync-sqlite-exec2-c.png`), but Close (`CancelBtnClick`, `src/EERSynchronisation.pas:221-224`, `ModalResult:=mrAbort`) does not close it, nor does Escape or Alt+F4 (`30-sync-sqlite-x-c.png`). The sqlite catalog's stage E (2026-09-07) still saw the exception `near "show": syntax error` in the centred exception dialog; that dialog no longer appears.
Expected: either a real SQLite implementation (sqlite #8) or an error message and a dialog that closes.
Suspected cause: `TDMDBEER.EERMySQLSyncDB` (`src/DBEERDM.pas:2222-2228`) issues `SET FOREIGN_KEY_CHECKS=0` through `DMDB.ExecSQL` (which swallows errors unless `RaiseOnError`, `src/DBDM.pas:701-722`; confirmed by reading) and then `show tables` through `SchemaSQLQuery.Open` - a syntax error on SQLite. Why the resulting exception is neither shown by `TMainForm.AppException` (`src/Main.pas:502`) nor lets the form close afterwards was **not** confirmed within the time box; candidates are the `try ... finally` at line 2360 plus the exception-collecting rewrite of round 2 (`ExecSyncStmt`, lines 2158-2172, `SyncErrors`) and the modal state of the form after an exception escaped `ShowModal` (`src/Main.pas:1533-1538`). Fix scope: small for the message/close (guard the SQLite case up front, as the Reverse Engineering dialog does), larger for real SQLite sync (#8).

Observations (not entries): (a) the connection selector keeps the password of the previous connect in the session; typing the password again appends to it (`20-connsel-c.png` shows 12 dots after two extra `bpsa`) and the connect then fails with "Access denied for user 'bpsa'@'localhost'" (`19-error-c.png`) - convenient when noticed, confusing when not; original behaviour, not filed. (b) `~/.DBDesigner4/DBConn.ini` is read once at start-up: connections added to the file while the app runs do not appear in the selector (`14-connsel-c.png` vs `15-connsel-c.png` after a restart) - not filed. (c) #21 check: the DB connection tree ("Network Hosts") and the connection grid show no permanent scrollbars (`14-connsel-c.png`); the Reverse Engineering dialog's table list has none either; the datatype-substitution memo (two lines) and the synchronisation progress memo show a permanent vertical scrollbar (`17-revdlg-c.png`, `22-sync-c.png`) - both are `TMemo`s with `ScrollBars = ssVertical` (`src/EERSynchronisation.lfm:251-256`, `src/EERReverseEngineering.lfm:377-382`), not tree views, so not part of #21; `SavedModelsTV` / `LMTreeView` not reached. (d) MySQL export separates the two `CREATE TABLE`s with 12 blank lines (`round8-mysql.sql`), the SQLite export with one - cosmetic, from the (empty) standard-inserts block; not filed. (e) Reverse-engineered columns get `Pos` 1-based and `Width="0" Prec="0"` instead of 0-based / `-1`; harmless. (f) The Reverse Engineering dialog offers "Use Datatype substitution" ticked with `int=INTEGER`, `dec=DECIMAL` for MySQL; with it, `int` came back as `INTEGER` (idDatatype 5) as in the model. (g) Menus were driven by clicking; GTK popup menus are unnamed X windows, found by diffing `xwininfo -root -tree` before/after the click.

Proposed grouping: #24 stands alone (`DBEERDM.pas` reverse-engineering query; same family as mysql #9). #25 belongs to sqlite #8 (MySQL-only sync) for the failure, but the missing message and the stuck Close are new and should be looked at with the round-2 sync error handling (`ExecSyncStmt` / `SyncErrors`, mysql #8) since that rewrite is the difference to the 2026-09-07 behaviour.

Recommended fix order: #25 (dialog cannot be closed - the user has to kill the program), then #24 (silent data loss on the round trip), then the cosmetic observations if at all.

Exercised / not reached (round 8): exercised - load of `round8.xml` via the command line; Tips dialog; File > Export > SQL Create Script for My SQL and SQLite (Target Data Base dropdown, Save Script to file via the GTK dialog); both roundtrip helpers; Database > Reverse Engineering with a MySQL and a SQLite connection (selector, password, Connect, dialog, Execute) into a fresh model each; File > New; File > Save As; Database > Database Synchronisation with MySQL (Execute, Close) and with SQLite (Execute twice, Close/Escape/Alt+F4); comparison with `compare_models.py` plus grep for comments and options. Not reached - building the model in the Table Editor / Relation Editor (done by script, see above), Database > Connect / Disconnect as separate menu items, Store Model in Database / Place Model (`SavedModelsTV`, `LMTreeView`), the "Apply changes to Model" direction of the sync, "Don't delete existing Tables" unchecked, query mode on either backend, the Export dialog's option check boxes, the connection editor (New Database Connection).

## Round 9 (2026-09-09): copy, paste, undo, two open models

Date: 2026-09-09 (30-minute time box, real display DISPLAY=:0, binary rebuilt from 0fb24d2 with `lazbuild --build-all`, WorkMode=0 during the session and restored to 2). Screenshots: `<scratchpad>/shots/round9/`; working model `shots/round9/r9.xml` (copy of `round8.xml`, saved several times during the session); stderr `shots/round9/run.log` (one line, the usual canberra message - no exception at any step). Scenario: single-table copy/paste on round8, rubber-band copy/paste of both tables plus the relation, undo after paste, relation delete, then File > Open `order.xml` as a second model and copy across. The second half was cut short by the time box and by my own driving (see "Exercised / not reached"): the two-model paste, the cross-model datatype check, the save prompt, Copy as Image / Select All / Center Model and the Place/Link Model dialog were not reached.

### 26. Paste: the FK index of a pasted child table loses its link to the pasted relation (`FKRefDef_Obj_id="-1"`)

Severity: functional (data damaged by copy/paste; the pasted FK index is an orphan - deleting or editing the pasted relation will no longer remove/update its index, and the relation can no longer be found from the index).
Steps: `r9.xml` (= round8.xml: `parent` 1:n `child`, FK column `child.parent_id`); rubber-band around both tables and the relation (status "4 Object(s) copied" - the rubber band also took the earlier `parent_1` copy); Ctrl+C; Ctrl+V (status "4 Object(s) pasted"); Ctrl+S; inspect the XML.
Observed: tables `parent_2`, `child_1` and relation `parent_child` (`ID="1323" SrcTable="1313" DestTable="1326" FKFields="id=parent_id"`) are pasted with fresh ids and the relation is drawn between the right copies (`07-canvas-s.png`); `child_1.parent_id` has `IsForeignKey="1"`; but `child_1`'s index `child_FKIndex1` is written with `FKRefDef_Obj_id="-1"` whereas the original `child_FKIndex1` has `FKRefDef_Obj_id="1296"` (grep of `r9.xml` after the save: `INDEX ID="1331" IndexName="child_FKIndex1" IndexKind="1" FKRefDef_Obj_id="-1"`).
Expected: the pasted index refers to the pasted relation (`FKRefDef_Obj_id="1323"`), as the original does.
Evidence: `r9.xml` (saved), `07-status.png`, `07-canvas-s.png`.
Suspected cause (confirmed by reading the code): `src/EERDM.pas:750` `TDMEER.AssignNewIDsToEERObjects` (called from `PasteMIClick`, `src/Main.pas:1450`) rewrites, for every old id, only `ID="..."`, `idColumn="..."`, `SrcTable="..."` and `DestTable="..."` (`src/EERDM.pas:767-773`); `FKRefDef_Obj_id="..."` is not in the list, so on load `src/EERModel.pas:9954`/`10168` reads the old relation id, which then does not exist in the model - or worse, in a model that already contains that id (paste back into the source model, where the old relation 1296 still exists, or after a second paste) points at the wrong relation. In this run the value came out as -1, which suggests the loader also resets an unknown id; not traced further.
Suggested fix scope: small - add `FKRefDef_Obj_id` to the replacement list in `AssignNewIDsToEERObjects`.
Status: fixed - two causes in `TDMEER.AssignNewIDsToEERObjects` (`src/EERDM.pas`). (1) The remap list lacked `FKRefDef_Obj_id` (index -> relation) and `FKRefDefIndex_Obj_id` (relation -> index); both are now remapped, plus ` Obj_id` of plugin data records; ids whose object is not in the clipboard are left untouched as before. (2) The `-1` itself came from `DMMain.ReplaceText` being case-insensitive: replacing `ID="1296"` also rewrote `FKRefDef_Obj_id="1296"` into `FKRefDef_Obj_ID="1307"`, an attribute name the loader does not know, so the `StrToInt` fallback wrote -1 (the same happened to `FKRefDefIndex_Obj_id`; it also explains why the relation got its new id before the parent's PRIMARY index). The remap now uses the case-sensitive `StringReplace`. Verified on the real display (`shots/fix26/`): rubber band of both tables plus relation, Ctrl+C, Ctrl+V, Ctrl+S -> `f26-after-paste.xml` has `child_1.child_FKIndex1 FKRefDef_Obj_id="1307"` and `RELATION ID="1307" SrcTable="1297" DestTable="1310" FKRefDefIndex_Obj_id="1315"`, every remapped id is a pasted object; the originals are unchanged; after undo + redo (`f26-after-redo.xml`) the object/column/index/relation lines are identical. See notes-to-myself.md "Fix: model-edit #26-#28".

### 27. DB Model palette keeps the pre-rename names of pasted tables (two `parent` nodes after pasting `parent_1`)

Severity: cosmetic / misleading (the tree cannot tell the original from the copy; clicking a node still selects the right object as far as tried).
Steps: `r9.xml`; select `parent`, Ctrl+C, Ctrl+V (canvas shows `parent_1`, status "1 Object(s) pasted").
Observed: DB Model tree lists `parent`, `parent`, `child` (`06-tree.png`); after the 4-object paste the tree reads `parent`, `parent_1`, `parent_1`, `parent`, `child`, `child` while the canvas has `parent`, `parent_1`, `parent_1_1`, `parent_2`, `child`, `child_1` (`09-tree.png`). The names in the tree are the names the objects had when the clipboard XML was loaded, i.e. before `PasteMIClick`'s unique-name loop renamed them. The saved XML has the renamed tables, so it is only the palette that is stale.
Expected: tree shows `parent_1`, `parent_1_1`, `parent_2`, `child_1`.
Evidence: `06-tree.png`, `09-tree.png`, `r9.xml`.
Suspected cause: the rename in `src/Main.pas:1478-1497` (`theTbl.ObjName:=s; theTbl.RefreshObj;`) happens after `EERModel.LoadFromFile` (`src/Main.pas:1466`) has already populated the palette; `RefreshObj` repaints the table but nothing asks the model palette to rebuild (the palette is refreshed through the `QEventType_RefreshModelPalette` path, `src/Main.pas:2267`, `src/EERDM.pas:237`). Not confirmed which call inside the load refreshes the tree.
Suggested fix scope: small - post the model-palette refresh (or call whatever `LoadFromFile` uses) once after the rename loop in `PasteMIClick`.
Status: fixed - `PasteMIClick` calls `DMEER.RefreshPalettes` (the same posted `QEventType_RefreshPalettes` that `LoadFromFile2` uses) after the unique-name loop. Verified on the real display: after the 3-object paste the DB Model tree reads `parent_1`, `parent`, `child_1`, `child` (`10-tree.png` / `14-full-s.png`), after a single-table paste `parent_2` appears (`20-24-trees.png`); the tree follows undo (three nodes gone) and redo. See notes-to-myself.md "Fix: model-edit #26-#28".

### 28. Paste is not an undoable action: after Ctrl+V the Edit menu offers "Undo Move Object(s)" and Ctrl+Z moves a table instead of removing the pasted objects

Severity: functional (undo history is wrong after a paste; the user cannot take a paste back with Undo and Ctrl+Z does something unrelated).
Steps: `r9.xml`; select `parent`; Ctrl+C; Ctrl+V; rubber-band all; Ctrl+C; Ctrl+V; open Edit menu; Ctrl+Z; Ctrl+Z again.
Observed: after the two pastes the Edit menu reads `Undo Move Object(s)` / `Redo` (disabled) (`08-editmenu-s.png`); Ctrl+Z leaves all six tables in place and only shifts the stacked `parent_2`/`parent_1_1` pair (compare `07-canvas-s.png`, `09-undo-c.png`, `10-state-c.png`; menu afterwards `Undo Move Object(s)` / `Redo Move Object(s)`, `11-editmenu-c.png`). Two more Ctrl+Z toggled the same pair (`1213.png`). No paste was ever undone. No table had been dragged by hand in this session.
Expected: `Undo Paste` (or `Undo New Object(s)`) that removes the pasted tables and relation; the DBDesigner 4 original logged the paste.
Evidence: `08-editmenu-s.png`, `11-editmenu-c.png`, `09-undo-c.png`, `10-state-c.png`, `1213.png`.
Suspected cause (partly confirmed): `PasteMIClick` (`src/Main.pas:1426-1516`) writes the clipboard to a file and calls `EERModel.LoadFromFile(..., False, True)` without any `LogAction`/`StartSubActionLog`; `LoadFromFile2` (`src/EERModel.pas:3576`) has no action logging either (grep of that range for `LogAction|LogSubAction` is empty). Where the `Move Object(s)` entry comes from was not traced - `StartSubActionLog(at_MoveObj)` is called from `AlignSelectedObjs` (`src/EERModel.pas:7167`), `:7536` and `:12918` (object drag); the position shift the paste applies to the selected objects (`AssignNewIDsToEERObjects(..., shiftObjectPositions=True)` plus `MoveToSavedPosition`) is the candidate. Not confirmed.
Suggested fix scope: medium - wrap the paste in `StartSubActionLog(at_NewObj)` / `LogSubAction` per pasted object (as `EERModel.pas:2660` does for a new table) and end it after the rename loop.
Status: fixed - new action type `at_PasteObj` (`src/EERModel.pas`, menu text "Paste Object(s)"): `PasteMIClick` logs one action with a sub action per pasted object carrying `GetObjAsXMLModel` (taken after the rename), undo deletes the objects by id (a relation already removed with its table is skipped), redo rewrites the XML to `undo.xml` and reloads it like the delete-undo does; the sub actions are logged in reverse component order because `RedoActions` walks them backwards - the first attempt loaded the relation before its tables existed and the FK column was then dropped as obsolete. `at_NewObj` was not reused: its redo calls `NewTable`, which cannot restore columns. The stray "Move Object(s)" was not the paste but the accidental double-click of the diagnosis run: the second click's mouse down opens the move action in `TEERObj.DoMouseDown`, the mouse up that would discard it goes to the modal editor; `DoDblClick` now calls `DeleteOpenAction` first. Verified on the real display (`shots/fix26/`): Edit menu after the 3-object paste reads `Undo Paste Object(s)` (`11-13-menus.png`), Ctrl+Z removes both tables and the relation (`12-undo-s.png` from the first build, identical behaviour after the reorder), menu then offers `Redo Paste Object(s)`, Ctrl+Y brings all three back with the FK column and index (`14-redo-s.png`, XML diff empty); single-table paste, Ctrl+X (confirmation, Return) + Ctrl+V of the cut table and a double-click on `child` + Escape all leave `Undo Paste Object(s)` as the current action (`21-25-26-menus.png`, `20-23-24-status.png`). See notes-to-myself.md "Fix: model-edit #26-#28".

Observations (not entries): (a) a double-click that lands on a table header while the second click is meant as a plain selection opens the Table Editor, and while that modal editor is open every Ctrl+C / Ctrl+V / Ctrl+O / menu click from the driver goes to the editor - Ctrl+V pasted the text clipboard into the Table Name field (`03-te-s.png`, name became "s copy, paste, undo and editing"; Escape discarded it, the XML still had `parent`). This is normal edit-control behaviour and the entry-#9 shortcut gating working as designed; noted because it invalidated two of my steps (see below). (b) Ctrl+S in the `order` model rewrote `bin/Examples/order.xml` without a prompt (git showed it modified, no table added; reverted with `git checkout`). (c) File > Open of a second model does not open a second top-level window: the same main window changes its title to `DBDesigner Fork - order` and the Windows menu lists `round8` and `order` with a bullet on the active one (`15-winmenu.png`); the DB Model tree, Navigator and Datatypes palette switched to `order` (`14-order-s.png`). (d) Single-table copy/paste is clean: `parent_1` keeps all nine columns with datatypes, params and comments, the PRIMARY and the UNIQUE index `parent_name_uq` with remapped `idColumn`s (`05-te-parent1-s.png`, `r9.xml` INDEX 1309/1310 -> columns 1299/1300), and gets a unique name (`parent_1`, then `parent_2`, `parent_1_1` for the copy of the copy).

Proposed grouping: #26 and #28 are both in the paste path (`PasteMIClick` -> `AssignNewIDsToEERObjects` -> `LoadFromFile`) and should be fixed together; #27 is the palette refresh after the same routine and can ride along in the same commit.

Recommended fix order: #26 (silent data damage on every paste of a relation), #28 (undo history wrong after paste), #27 (cosmetic).

Exercised / not reached (round 9): exercised - load of `r9.xml` via the command line; single-table Ctrl+C / Ctrl+V with status messages, Table Editor of the pasted table, Ctrl+S and XML inspection; rubber-band selection of two tables plus relation (plus the earlier copy), 4-object copy/paste, XML inspection of the pasted relation and FK index; Edit menu state after paste (Copy/Cut/Paste/Delete/Select All/Center Model enabled, Undo caption); Ctrl+Z three times after paste; Ctrl+Del on a relation (the line click did not select it twice - no confirmation appeared and nothing was deleted, so relation delete + undo is NOT covered); File > Open (Ctrl+O, GTK dialog with typed path) of `order.xml` as a second model, Windows menu listing, palette/tree/title following the newly opened model. Not reached - cut + paste, paste twice in a row with the same clipboard, paste with an empty clipboard, undo after cut / table move / relation delete / table-with-relation delete / Table Editor change / Relation Editor change / create table+relation, redo of any of them, save+reopen after undo; copying from `order` into `round8` and back (my Ctrl+click selection double-clicked `productgroup` and opened its Table Editor, which then swallowed the Ctrl+C and the Windows-menu switch - `19-te-c.png`; the copy never happened), foreign datatype ids after a cross-model paste, closing a model with unsaved changes, alternating edits with palette/Navigator/title check, undo isolation between the two models; Copy as Image, Select All, Center Model, Delete with multi-selection; File > Add/Link Model and the Place/Link Model dialog (entry #21's LMTreeView scrollbars still unverified, the round-7 Escape note still open).

## Round 9b (2026-09-09): undo/redo, cross-model paste, Place Model

Date: 2026-09-09 (30-minute time box, real display DISPLAY=:0, binary rebuilt from 0226c32 with `lazbuild --build-all`, WorkMode=0 during the session and restored to 2 from `settings.bak`). Screenshots: `<scratchpad>/shots/round9b/`; working model `shots/round9b/r9b.xml` (copy of `round8.xml`, saved twice; `1a-after-undo.xml` is a copy of the first save), `shots/round9b/order9b.xml` (copy of `order.xml`, never opened); stderr `shots/round9b/run.log` (no exception; one `Gdk-WARNING ... GdkWindow 0xc0023c unexpectedly destroyed` when I closed the Tips window with `xdotool windowclose`, one pair of `GLib-GObject-CRITICAL ... no emission of signal "key-press-event" to stop` at the first Ctrl+Del - see #30). `git status` clean apart from this file. Driver helpers: `shots/round9b/h.sh`.

The round produced almost no scenario coverage: 20 of the 30 minutes went into clicks that never reached the canvas (see #29, which is the real finding of this round), and the one relation-delete attempt made after the cause was removed still did not delete anything (see #30, unconfirmed). Nothing after step 1(a) was reached.

### 29. The "DBDesigner Fork Tips" window opens on top of the model canvas at start-up and silently takes every click that lands on it (tables/relation under it cannot be selected)

Severity: usability / test-blocking (nothing is lost, but with the default geometry the tips window covers the area where a freshly loaded small model is drawn, it is not modal, and a click on a table header or relation line that is under it does nothing - no selection, no error, Edit > Copy/Delete stay disabled; the user sees the table and clicks it, the tips window does not even come to the front because it is already on top).
Steps: `bin/DBDesignerFork <scratch>/r9b.xml` with the saved 1878x886 main-window geometry; the tips window appears at `469x272+423+295` (`xwininfo`), i.e. over screen x 423..892 / y 295..567, which is exactly where `parent` (header at 374,326 - just outside), the relation line (502,397), its label (578,356) and `child` (header at 794,326) are drawn. Click the `child` header once, open Edit; click the relation line, open Edit.
Observed: `xdotool getmouselocation` at (794,326) reports `window:12583484` = 0xc0023c "DBDesigner Fork Tips", not the main window; Edit menu after the header click: `Copy selected Object(s)`, `Cut`, `Delete selected Object(s)` all disabled (`1b-m4c.png`, also `1a-m2c`/`1a-m3c` after the line/label clicks, `1a-menu0.png` for the whole menu), Ctrl+C gives no "copied" status (`1b-status.png`), no selection highlight on the table (`1b-c100z.png`), and a double-click on the same spot does not open the Table Editor (window list `1b` step: only the main window and the tips window are viewable). After `xdotool windowclose 0xc0023c` the same coordinate reports `window:12583464` (main window). The tips window in the window list is the "viewable but not modal" one that the round-9 fix notes mention as harmless; it is harmless for keyboard shortcuts only.
Expected: the tips window should either be modal/centered over the main form and dismissed before work (the DBDesigner 4 original shows it as a modal dialog with a "Show tips at startup" box), or open beside the canvas, or at least be raised/focused so that the user understands why the click did nothing. Also the round-9 note "the line click did not select it twice" was most likely this, not a relation-selection defect.
Evidence: `1b-m4c.png` (Delete disabled after header click), `1a-m2c.png`, `1a-m3c.png`, `1b-c100z.png`, `1b-status.png`, `xdotool getmouselocation` output quoted above.
Suspected files: the tips form is shown from the main form start-up (`src/Main.pas`, search for the Tips form / `ShowTips`), position/modality not checked in the code - not confirmed.
Suggested fix scope: small (ShowModal or `Position:=poMainFormCenter` + `FormStyle`), but check whether the tips form is deliberately modeless in the original.
Status: fixed - the original (first commit `Tips.xfm`, `Main.pas`) is the same modeless `Show` with `fsStayOnTop` + `poMainFormCenter`, so the port only inherited it; it was never modal. Kept modeless (no `ShowModal`: the self-test starts from OnIdle after `ShowPalettesTmrTimer` returns, a modal loop there would block it, ui-bug #5): `src/Tips.lfm` now `FormStyle = fsNormal`, `Position = poDesigned`; `src/Main.pas` `ShowPalettesTmrTimer` makes the form a `pmExplicit` popup child of the main form and, after `Show`, moves it to the lower-right corner of the main window (`Left+Width-TipsForm.Width-24`, `Top+Height-TipsForm.Height-48`). Two GNOME Shell findings decided the shape: a plain `fsNormal` window is put *behind* the maximized main form at start-up (`_NET_WM_STATE_DEMANDS_ATTENTION`, never seen), and a transient child is always kept above its parent and is centred over it when mapped (the position is only honoured after it is on screen) - so "goes behind on the first click" is not available here; "visible but off the canvas centre" is. Verified on the real display (`shots/fix29/`): tips window at `1172,683` (lower right) instead of `437,400`; `xdotool getmouselocation` at (600,500) - inside the old rectangle - reports the main window with the tips open; `child` dragged there and clicked shows the selection outline (`main4-z.png`); the tips window's own Close button destroys it and the app stays up; with `ShowTipsOnStartup=0` only the main window is viewable. `xvfb-run -a ./bin/DBDesignerFork --selftest`: 168 tests, PASS 93, FAIL 0, Phase 0 still logs `Closing: TipsForm`. Left open: the corner offset is approximate (the WM/LCL frame sizes shift it by a few tens of pixels), and the tips form still takes the keyboard focus at start-up as the original did. See notes-to-myself.md "Fix: model-edit #29".

### 30. (unconfirmed) Ctrl+Del on a clicked relation deletes nothing and logs no action

Severity: functional if real (relation delete by keyboard would be impossible); confidence low - only one attempt after #29 was removed, and the selection state was not verified in that attempt.
Steps: after `windowclose` of the tips window: `windowactivate --sync`, single click on the relation line at (502,397) (the line's exact y, found from the 4x zoom `1a-linez.png`), wait 0.8 s, Ctrl+Del, wait 1 s, list windows, Return, capture.
Observed: no Confirmation window appears (window list: main window only), the relation is still drawn (`1a-chk.png`, top), the Edit menu reads `Undo` / `Redo` with no action caption (`1a-chk.png`, bottom), Ctrl+Z then Ctrl+S: `r9b.xml` still has `RELATION 1296`, `parent_id` and `child_FKIndex1` (so at least nothing was damaged); Ctrl+Y likewise (`1a-redo.png`). The GLib-CRITICAL pair in `run.log` was logged at the very first Ctrl+Del of the session (13:23:48, while the tips window still had the pointer) - a `key-press-event` stop on an instance that has no handler; not seen again.
Expected: relation selected (highlighted), Ctrl+Del asks for confirmation and removes the relation, `child.parent_id` and `child_FKIndex1`; Edit menu then `Undo Delete Object(s)`.
Not confirmed: whether the click selected the relation (the Edit menu was not opened between the click and Ctrl+Del in this attempt; `TEERRel.DoMouseDown` at `src/EERModel.pas:10729` toggles `SetSelected(Not(Selected))` after `DeSelectAllObjs`, so a click on the 2-px line should select it if it reaches the control). Possible mundane causes: the click after `windowactivate` was lost (known trap), or the line hit-test missed by a pixel. Needs a retry with the Edit menu checked after the click before it can be filed as a bug; kept here so the next round starts from it.
Evidence: `1a-chk.png`, `1a-final.png`, `1a-after-undo.xml`, `r9b.xml`, `run.log`.
Suspected files: `src/EERModel.pas:10729` (`TEERRel.DoMouseDown`), `src/EERModel.pas:5222` (`DeleteSelectedObjs`), `src/Main.pas:1068` (`DeleteMI.Enabled:=HasSel and Not(InEdit)`).

Observations (not entries): (a) with nothing logged yet the Edit menu shows bare `Undo` / `Redo` captions, both enabled-looking in the GTK popup (`1a-menu0.png`) - whether they are really enabled with an empty log was not tested. (b) `import -window` of the main window returns 1878x923 for an 1878x886 client (title bar included), and the Edit-menu popup is the separate 300x241 `DBDesignerFork` window, as the round-9 notes say; `menushot` in `h.sh` finds it. (c) The first click after `xdotool windowactivate --sync` was lost at least once more (the Edit menu did not open, `1a-m1` step).

Driver warning for the next round (not an app bug): after `xdotool windowclose` on the tips window (13:33, XDestroyWindow, not a WM close) the main window of that process went `IsUnMapped` (`Gdk-WARNING ... GdkWindow 0xc00300 unexpectedly destroyed`), and every later launch in the session (13:35, 13:38, both with the model loaded and the tips window created) came up with all windows unmapped and `xdotool getactivewindow` failing with `_NET_ACTIVE_WINDOW ... code=1`, while `loginctl` reported the session active and not locked - the X side of the display was unusable for the rest of the box, which is why nothing after 1(a) was reached. Do not `windowclose` the tips window; `xdotool windowmove <tips-xid> 1300 650` (or its own Close button) is the way to get it off the canvas.

### Round 9b retry (2026-09-09, "round 9c"): results

Retry with the tips window disabled (`ShowTipsOnStartup=0` in the settings ini during the session, restored afterwards; WorkMode=0, restored to 2 from `shots/round9c/settings.bak`), binary rebuilt from 0226c32 with `lazbuild --build-all` (0 errors). Screenshots: `<scratchpad>/shots/round9c/`; working models `shots/round9c/r9c.xml` (copy of round8.xml) and `shots/round9c/order9c.xml` (copy of order.xml), both saved several times; stderr `shots/round9c/run.log` (only the canberra line - no exception, no GLib warning at any step). Main window resized to 1600x886 at +42+69 with `xdotool windowsize` (the saved geometry was not applied - see observation (e)). `git status` clean apart from this file.

#29 addendum (human impact, from `src/Tips.lfm`:12-13 and `src/Main.pas:1783-1792`): the tips form is created with `FormStyle = fsStayOnTop`, `Position = poMainFormCenter` and shown with `TipsForm.Show` (modeless). So for a human user it behaves exactly as for the driver: it sits centred over the canvas, stays above the main window even when the main window is clicked, is not modal (the canvas still takes keys), and a click that lands on it goes to the tips form - the user has to close or move it before selecting anything under it. Fix decision: either `ShowModal` (as in the DBDesigner 4 original) or keep it modeless but drop `fsStayOnTop` and place it `poDesktopCenter`/beside the canvas; with `fsStayOnTop` the round-9 "the line click did not select it twice" and the whole 9b box are explained.

#30 resolution: **not a bug** - relation delete by keyboard works. With the tips window disabled, a single click on the relation label at its true position (screen 578,355 - the 9b coordinates were derived from a capture that included the 49-px window frame, so both the 9b label click and the 9b line click were ~50 px off, on top of the tips-window problem) selects the relation (dotted outline, `03-label-z.png`); Ctrl+Del opens the `Confirmation` window (405x154), Return removes the relation and the FK column `child.parent_id` from the canvas and the Edit menu reads `Undo Delete Object(s)` (`04-both.png`). The 9b observation was a missed click, not a defect. `src/EERModel.pas:10729` needs no change.

Scenario results (all on the real display, all without exception):
- 1(a) relation delete / undo / redo: Ctrl+Z restores the relation and `parent_id (FK)` on the canvas and the tree keeps `parent`, `child`; menu `Undo` (bare) / `Redo Delete Object(s)`; Ctrl+Y removes both again; after a further Ctrl+Z and Ctrl+S the XML has `RELATION ID="1296" ... FKFields="id=parent_id"`, `parent_id` and `child_FKIndex1 FKRefDef_Obj_id="1296"` (`0506.png`, `r9c.xml`). OK.
- 1(b) delete `child` (table with the relation): Confirmation, table and relation gone, tree shows `parent` only (`0709.png` top); Ctrl+Z brings `child`, the relation and `parent_id (FK)` back, tree `parent`, `child`, menu `Redo Delete Object(s)`; Ctrl+Y deletes again; after undo + save the XML has both tables, the relation, the FK column and index (grep count 3). OK.
- 1(f) move `parent` by 200 px (mouse drag): menu `Undo Move Object(s)`, Ctrl+Z puts it back, Ctrl+Y moves it again (`1013.png`). OK.
- 1(g) redo stack after a new edit: undo the move, then delete the relation (new edit): Edit menu reads `Undo Delete Object(s)` / bare `Redo`, and Ctrl+Y afterwards changes nothing (`13-menu-c` and `13-afterY` in `1013.png`). OK - the redo stack is cleared.
- 2 cross-model paste order -> round8: `order9c.xml` opened with Ctrl+O; `product` + Ctrl-click `productgroup` + Ctrl-click the `ProductgroupRef` label, Ctrl+C ("3 Object(s) copied to clipboard"); Windows menu -> round8; Ctrl+V ("3 Object(s) pasted from clipboard"): both tables, all columns, the relation and the tree entries `product`, `productgroup` appear, menu `Undo Paste Object(s)` (`17all.png`). Datatype check: the pasted columns keep `idDatatype` 5/34/33/8/28/26 and in both files these ids are INTEGER, Varchar(45), Varchar(20), FLOAT, TEXT, LONGBLOB - identical because both models carry the same standard datatype table (ids 1-36). A column that uses a model-local datatype (round8 has ids 38-51, e.g. CHAR(1)/CHAR(15), but no copied column uses them) was NOT exercised; since `AssignNewIDsToEERObjects` leaves `idDatatype` alone by design (notes-to-myself "Fix #26"), pasting such a column into a model without that id remains an open question (would need a custom datatype on a copied column).
- 2 reverse paste round8 -> order: `parent` (9 columns, standard datatypes 5/20/12/14/28/22/7/15/6) pasted into order with the same ids and names (`19all.png`, `order9c.xml`). OK.
- 2 undo isolation: Ctrl+Z in order removes the pasted `parent` from the canvas (menu bare `Undo` / `Redo Paste Object(s)`); switching to round8 shows `parent`, `child` and the pasted `product`/`productgroup` unchanged with `Undo Paste Object(s)` still current (`2021.png`). OK - but see #31 for the close prompt and observation (d) for the tree.
- 2 close with unsaved changes: see #31 (no prompt after undo); with an ordinary change (relation deleted) File > Close shows the "The model has not been saved. Do you want to save the model before closing?" box with Cancel / No / Yes (`2425.png` top); Cancel keeps the model open. Yes/No paths not exercised.
- 4 Add/Link Model from File: see #32; Escape/close: see #33. Selection/Place menus, LMTreeView scrollbars (#21) and placing + undo could not be reached because the model was never accepted for placing.
- 1(c) Table Editor undo granularity, 1(d) Relation Editor undo, 1(e) Ctrl+T + relation tool undo, 3 (Copy as Image, Select All, Center Model, multi-selection delete with region/note) were not reached in the box.

### 31. Closing a model whose only unsaved change is an undo (or redo) does not ask to save - the undone state is silently discarded

Severity: functional (data loss path: save, undo one or more steps, close -> the file on disk keeps the state the user just undid, no prompt).
Steps: open `order9c.xml` (second model), paste `parent` into it, Ctrl+S (status "The model was successfully saved"), Ctrl+Z (`parent` disappears from the canvas, Edit menu `Redo Paste Object(s)`), File > Close.
Observed: the model closes immediately; the main title switches to `DBDesigner Fork - round8`, the Windows menu lists only `round8` (`23.png`), no Confirmation/save dialog was created (window list after the click: main window only). `order9c.xml` on disk still contains `parent`. Control: after a relation delete in round8 (no save in between) File > Close does show the Cancel/No/Yes box (`2425.png`).
Expected: the close prompt, because the in-memory model differs from the file.
Evidence: `20-undo.png` (order after undo, `parent` gone), `20-menu-c` in `2021.png`, `23.png`, `order9c.xml` (has `Tablename="parent"`), `2425.png` (prompt in the control case).
Cause (confirmed by reading the code): `TEERForm.FormCloseQuery` (`src/EER.pas:332-354`) prompts only if `EERModel.IsChanged`; `IsChanged` is set by `TEERModel.ModelHasChanged` (`src/EERModel.pas:5429`) and cleared on save (`:1344`), but neither `UndoActions` (`src/EERModel.pas:5525`) nor `RedoActions` (`:5725`) nor any code in 5525-5990 calls `ModelHasChanged` (grep of that range is empty). Every action type is affected, not only paste.
Suggested fix scope: small - call `ModelHasChanged` at the end of `UndoActions` and `RedoActions` when at least one action was rolled back / re-applied (a nicety: compare the current action index with the one at the last save to clear the flag when the user undoes back to the saved state).
Status: fixed - `UndoActions`/`RedoActions` now call `ModelHasChanged` when at least one action was rolled back / re-applied (the original tracks no saved position in the action log, so undoing back to the saved state also marks the model changed - a prompt too many, never one too few). Verified on the real display with a table move: Ctrl+S, Ctrl+Z, File > Close shows the Cancel/No/Yes prompt and Cancel keeps the model open (`fix31/04-prompt.png`); Ctrl+S, Ctrl+Y, Close prompts too; Ctrl+S then Close on the unchanged model closes without a prompt (`fix31/0506.png`). See notes-to-myself.md "Fix: model-edit #31".

### 32. Add/Link Model from File opens the picked file as a second full model (Windows menu, title, palettes switch to it) and then refuses it with "You must not place a Model on itself."

Severity: functional (the Place/Link feature is unusable from a file: nothing can be placed, and the user is left with an extra open model he did not ask for; possibly the same for the Database/Online Library sources - not tested).
Steps: round8 active (`r9c.xml`), only model open (Windows menu `round8`, `23.png`); File > Add/Link Model > Add/Link Model from File (this only selects a placement tool - `AddLinkModelFromFileMIClick`, `src/Main.pas:3787`, calls `DMEER.SetWorkTool(wtPlacementFromFile)`; nothing visible happens until the canvas is clicked - see observation (b)); click an empty spot of the canvas; the GTK "Open a Model ..." dialog appears; type the path of `order9c.xml`, Return.
Observed: the main window title changes to `DBDesigner Fork - order`, the canvas shows the whole order model (with the `parent` copy saved earlier), the DB Model tree lists order's tables, the Windows menu now lists `round8` and `order` with the bullet on `order` (`29.png`, `3334.png`), and an `Error` box "You must not place a Model on itself." pops up at the top-right corner of the screen (1625,69 - not centred, `29-error.png`). After OK the empty `Place / Link Model` window (700x576, only the File/Selection/Place menu bar, no tree, no settings panel, `31-place-s.png`) is shown modally. File > Load Model from File inside that window with the same file gives the same error (`33-error.png`), although this time the model is drawn inside the dialog (`33-place-s.png`). File > Abort closes the dialog; `order` stays open as a second model afterwards (`3334.png` bottom).
Expected: the file is loaded only into the placement dialog's private `Model2Place`, the active model stays `round8`, the dialog shows the order model with its tree and settings, and the "on itself" check compares against `round8`.
Evidence: `29.png`, `29-error.png`, `29-main.png`, `31-place.png`, `33-error.png`, `33-place.png`, `3334.png`, `23.png` (before).
Suspected cause (not confirmed): `TEERPlaceModelForm.LoadModelfromFile` (`src/EERPlaceModel.pas:201-240`) creates `Model2Place:=TEERModel.Create(self)` and calls `Model2Place.LoadFromFile2(fname)` with the default `ReadSettings=True`; something in that load path registers the model / the loaded file as the active document (title, Windows menu entry, palettes all followed it), after which `Model2Place.ModelFilename=EERModel.ModelFilename` (`:216`) is true because `EERModel` is (or reports) the just-loaded order. `TEERModel.Create` (`src/EERModel.pas:1205`) itself contains no MainForm/RegisterEERForm call (grep), and `LoadFromFile2` posts `QEventType_RefreshPalettes` (`src/Main.pas:1727`, `:2154`) - which explains the palettes but not the Windows-menu entry. Where the second TEERForm/menu entry comes from must be traced (`QEventType_AddChildFormsMenuItem`, `MainForm.RegisterEERForm`, the `wtPlacementFromFile` mouse-down handler at `src/EERModel.pas:1658`/`:13301`).
Suggested fix scope: medium - find the registration side effect and make the placement load private (or load through `LoadFromFile2(fname, False, ...)` and set the filename by hand); then verify the on-itself check with two files.
Status: not reproduced as described, hardened - with a correctly delivered file name the port already loads the picked file only into the dialog's `Model2Place` (probes in `LoadModelfromFile`: source = picked file, target = active model, no `OpenFile` call, no second `TEERForm`), in both directions (`order` <- `round8`, `round8` <- `order`) and after `order` had been opened and closed as a second model beforehand; the placement itself works (Selection > Select All, Place > Add selected Objects put `parent`, `child` and `parent_child` on the target canvas at the click position, `fix32/after-c.png`; the original logs no action for it, so Ctrl+Z does not remove them). The round-9c "second model" therefore came from the driver, not the code: the GTK "Open a Model ..." dialog pre-fills its Location entry with the active model's file name (`fix32/opendlg-s.png`), and text typed without a Ctrl+A is appended to it - in this session that produced `round8.xmlorder.xml` (`run.log`), a nonexistent name the dialog returned anyway; `LoadFromFile2` then raised inside `SetData`, the exception was swallowed and the empty dialog of `31-place-s.png` opened. What was fixed (`src/EERPlaceModel.pas` `LoadModelfromFile`): the on-itself check now compares `ExpandFileName` of the picked name with the target's *before* anything is loaded and returns False (no empty dialog after the error, `fix32/on-itself.png`, centred); a missing file is reported as "File not found:"; an exception from `LoadFromFile2` is caught, reported with the file name and the message, `Model2Place` is freed and the function returns False so `SetData` does not open the dialog. Verified on the real display with the `order` copy as target and the `round8` copy as source (`fix32/before.png`, `dialog-loaded.png`, `after.png`): no error, no second window, the Windows menu keeps one entry, the dialog shows the round8 preview. `LMTreeView` (#21) is not shown on the from-file path (`LMPnl` is `Visible = False`; the tree only appears through Add/Link Model > Linked Models, `DisplayLinkedModels`) - still unverified. See notes-to-myself.md "Fix: model-edit #32, #33".

### 33. The Place / Link Model dialog cannot be closed with Escape or the window-manager close: only File > Abort ends it

Severity: usability (the round-7 "Escape note" - now confirmed; with #32 the user reaches this dialog in an empty state and has to discover the File menu to leave).
Steps: reach the dialog as in #32; press Escape; press Alt+F4.
Observed: the window stays (`vis` list after each key: `Place / Link Model` 700x576 still viewable); the form has `BorderStyle = bsSizeToolWin` (`src/EERPlaceModel.lfm:4`) - a tool window without a close button in this WM - and no `OnKeyDown`/`OnCloseQuery` handler (grep of `src/EERPlaceModel.pas` for `Escape|KeyDown|CloseQuery`: none); File > Abort (`ModalResult:=mrAbort`, `src/EERPlaceModel.pas:363`) closes it.
Expected: Escape (a Cancel/Abort button with `Cancel=True`, or `KeyPreview` + `VK_ESCAPE`) and the WM close both abort.
Evidence: `31-place-s.png`, `32-pfile.png` (File menu: Load Model from File / Load Model from Database / Abort), window lists in the transcript.
Suggested fix scope: small.
Status: fixed - `src/EERPlaceModel.lfm` gets `KeyPreview = True`, `OnKeyDown = FormKeyDown` (plain Escape sets `ModalResult:=mrAbort`, same as File > Abort) and `OnCloseQuery = FormCloseQuery` (a WM close of the modal form sets `mrAbort` when no result is set; the caller frees the form after `ShowModal`, so no `caFree`). `BorderStyle` left at `bsSizeToolWin`; `xprop` shows `_NET_WM_ACTION_CLOSE` and `WM_DELETE_WINDOW` on the window, so the WM does offer a close. Verified on the real display: Escape closes the loaded dialog (window list afterwards: main only); File > Abort still closes it. Alt+F4 sent with `xdotool key alt+F4` (also `--clearmodifiers`) did nothing under GNOME Shell - the same driver result as round 9c, and most likely the shell ignoring the synthesized chord for the utility-type window rather than the handler; the `OnCloseQuery` path is unverified by this driver. See notes-to-myself.md "Fix: model-edit #32, #33".

Observations (not entries): (a) with `ShowTipsOnStartup=0` the whole session ran without a single lost click after `windowactivate --sync`; the earlier "first click lost" trap was not seen once. (b) Add/Link Model from File gives no feedback that a tool was armed: the status bar still reads the pointer hint and nothing opens until the canvas is clicked (`28-status.png`); the status hint could name the placement tool. (c) The error box of #32 opens at the top-right screen corner (1625,69), the Confirmation boxes open centred - inconsistent `Position` of `MessageDlg` vs the app's confirmation form; cosmetic. (d) Right after switching to round8 through the Windows menu (1.5 s later) the DB Model tree still listed order's tables (`parent`, `product`, `onlineorderhasproduct`, `onlineorder`, `weblog`, `21-tree.png`) while the title and canvas were round8; a later capture (`23.png`, after a Close in the other model) showed the right tree, so it is a transient stale palette after a Windows-menu switch - low confidence, one observation, not filed. (e) The saved window geometry (1878x886 in round 9b) was not applied at start-up: the main window came up 600x426 (`xwininfo`), the palettes were resized with it; not investigated. (f) A cross-model paste places the objects at their source coordinates (product/productgroup landed at the bottom-left of the round8 canvas, far from the two existing tables) rather than near the current view - same as the in-model paste offset rule, acceptable.

Proposed grouping: #29 alone (start-up/tips form: `src/Tips.lfm`, `src/Main.pas:1783`); #31 alone (`UndoActions`/`RedoActions` change flag); #32 + #33 together (Place/Link Model: `src/EERPlaceModel.pas` and the placement-tool load path) - #21 (LMTreeView scrollbars) will only become testable once #32 is fixed. #30 closed as not a bug.

Recommended fix order: #31 (silent data loss with a two-line fix), #32 (feature unusable), #29 (blocks work on small models and every driver run), #33 (ride along with #32).

Exercised / not reached (round 9b + retry): exercised - rebuild from 0226c32; relation select by label click with Edit-menu verification, Ctrl+Del + Confirmation, undo/redo of relation delete, table-with-relation delete and table move with canvas/tree/menu/XML checks; redo-stack clearing after a new edit; second model via Ctrl+O; Ctrl-click multi-selection of two tables plus relation, cross-model copy/paste in both directions with datatype-id/name comparison in the saved XML (standard datatypes only); undo of the cross-model paste and undo isolation between the two models; File > Close with and without a real change (prompt seen with Cancel/No/Yes, Cancel path); Add/Link Model from File tool + canvas click + file dialog, the Place/Link dialog's File menu, Load Model from File inside it, Escape/Alt+F4/Abort. Not reached - 1(c) Table Editor session undo granularity, 1(d) Relation Editor change undo, 1(e) Ctrl+T + relation-tool undo/redo; save prompt Yes/No paths; paste of a column using a model-local datatype id; 3 (Copy as Image, Select All, Center Model, multi-selection Delete with region + note, undo); 4 beyond the load error (Selection/Place menus with a model, LMTreeView scrollbars #21, placing + undo). Round-7's Escape note is now #33.

## Regression pass (2026-09-09, HEAD d0fbc19, rounds 6-9b combined)

Date: 2026-09-09, 35-minute time box, real display DISPLAY=:0, binary rebuilt with `lazbuild --build-all` from d0fbc19. All work on copies under `<scratchpad>/shots/regress/` (`round8.xml`, `order.xml`, new `s1.xml`, `rev.xml`, `s1-mysql.sql`, `s1-sqlite.sql`, driver `h.sh`, stderr `run.log` - no exception, no GLib/GTK critical during the whole run). Purpose: replay the round 6-9b scenarios on one build, since every fix had only been verified alone. Nothing was changed in the code.

| Entry | Result | Evidence (`shots/regress/`) |
|---|---|---|
| #15 string-input prompt readable | verified | `15-all.png` (bottom: "Name of Index:" fully visible next to the edit) |
| #16 Set Datatype popup submenu | verified | `07-both.png`, `08-sub-s.png`, `09-sub2-s.png`, `10-comments-c.png` (col3 = DATETIME via popup > Date and Time Types) |
| #17 RAID Type shows STRIPED | verified | `16-adv-c.png` |
| #18 no scrollbars on the Table Editor page tree | verified | `16-adv-c.png`, `04-editor-s.png` |
| #19 rename table away and back keeps its name | verified | `15-all.png` (middle: `Table_01` -> `foo` -> `Table_01`) |
| #20 comment typed after clicking out of the new-row name editor | verified | `10-comments-c.png` (`comment three` complete) |
| #21 tree views without permanent scrollbars | verified | `00-main-s.png`, `32-all.png` (DB Model tree, no bars) |
| #22 Shift+click extends the grid row selection | verified | `11-multisel-c.png`, `13-multi-c.png` (rows 2-3 both set to BIGINT by one popup action) |
| #23 SQL export skips empty indexes, no FULLTEXT for SQLite | verified | `s1-sqlite.sql`, `s1-mysql.sql` (`Table_01_index1280` without columns is in `s1.xml` but in neither script) |
| #24 reverse engineering keeps column/table comments and InnoDB | verified | `rev.xml` (`TableType="1"`, `Comments="table comment child"`, 12 column comments), `52-rev-s.png` |
| #25 sync error shown on top, dialog closable | verified (SQLite half) | `54-err-s.png` ("Synchronisation aborted ... SQLite ... not supported yet"), dialog closed with Escape afterwards (window list in the driver output); MySQL ALTER sync not reached |
| #26 pasted FK index keeps its relation link | verified | `round8.xml` after paste: `FKRefDef_Obj_id="1307"` on `child_1`'s FK index = pasted `RELATION ID="1307"`, no stray `-1` beyond the non-FK indexes |
| #27 DB Model tree shows the final pasted names | verified | `32-all.png` (top: `parent_1`, `parent`, `child_1`, `child`) |
| #28 paste is one undo action | verified | `32-all.png` (Edit menu `Undo Paste Object(s)`), `35-all.png` (Ctrl+Z removes all three objects, Ctrl+Y restores them) |
| #29 tips window parked lower-right, canvas clickable | verified | `60-tips.png`; `xwininfo`: tips at (1440,659) 469x272 on a 1920x955 screen, main window at (42,69) 600x426 - no overlap, canvas centre click went to the model |
| #30 (unconfirmed) Ctrl+Del on a clicked relation | verified working | `41-all.png`: label click selects the relation, Ctrl+Del asks for confirmation, child loses `parent_id (FK)`, Ctrl+Z brings relation and FK column back |
| #31 close after undo asks to save | verified | `35-all.png` (bottom: "The model has not been saved" after Ctrl+S, Ctrl+Z, File > Close; Cancel kept the model open) |
| #32 Add/Link Model loads the picked file into the dialog only | not verified | the driver could not activate the `Add/Link Model` submenu item (mouse click and Return on the highlighted item both closed the menus without opening the file dialog, `61-sub.png`); not a product observation, no time left to retry |
| #33 Place Model dialog closes with Escape / WM close | not verified | same as #32 |

S1 (field editing, rounds 6/7): File > New, two tables via Ctrl+T, Table Editor on `Table_01`: `col1 VARCHAR(20)` typed, `col2 BOOL` from the in-cell drop-down (`06-both.png`), `col3 DATETIME` from the popup submenu, three comments (the third typed after clicking out of the open row-4 name editor), Shift+click rows 2-3 + popup > Numeric Types > BIGINT applied to both, table renamed `foo` and back, index `Table_01_index1280` created from a readable prompt, Advanced page correct, OK; `Table_02` with `id2`; relation drawn with the palette tool (the n:m button was hit instead of 1:n, so `Table_01_has_Table_02` was created - tool placement works either way); Ctrl+S to `s1.xml`: three tables, two relations, all three comments, datatype ids 20/6/6 persisted. Not reached: reopening `s1.xml` in the editor (the XML grep was taken as persistence evidence) and a real 1:n relation.
S2 (paste/undo, rounds 9/9b): everything in the table above passed on the `round8.xml` copy. Note that a plain `Delete` key on a selected relation does nothing - the menu shortcut is Ctrl+Del, so this is by design, but it cost one attempt.
S3 (database, round 8): `tests/mysql-roundtrip.sh round8-mysql.sql regress_r8` loaded without errors (`mysql-roundtrip.out`, both tables InnoDB, all comments in `information_schema`); the reverse engineering ran against the stored `OrderMySQL` connection (`dbdtest`, which holds the same round-8 tables with comments - the app had read `DBConn.ini` at start-up, so re-pointing it at `regress_r8` did not take effect for this run) and the saved `rev.xml` carries the comments and `TableType="1"`. SQLite sync: error on top, dialog closable. Not reached: Database Synchronisation of a modified copy against MySQL (ALTER listing). `regress_r8` was dropped afterwards.
S4 (start-up and dialogs, rounds 6/9b): tips window verified as above; string-input prompt verified in S1; Add/Link Model not reached (driver problem, see #32/#33).

Self-test (`xvfb-run -a ./bin/DBDesignerFork --selftest`, `selftest.log`): PASS 93, FAIL 0.

### 34. (unconfirmed) Typing a datatype into an already filled DataType cell after a single click does not replace it

Severity: cosmetic/usability if real; confidence low - one attempt inside the time box, the cell editor state was not captured.
Steps: Table Editor, `col3` already `BIGINT` (set through the multi-row popup); single click on the DataType cell of row 3, Ctrl+A, type `DATETIME`, Return.
Observed: the cell still reads `BIGINT` afterwards (`15-all.png`, middle grid, `col3`); the same typed sequence into a freshly created row (`col1`, `VARCHAR(20)` typed straight after Tab from the name editor) did replace the default `INTEGER` (`06-both.png`).
Expected: either the click opens the in-place editor so that Ctrl+A + text replaces the value, or a second click / F2 is required and documented; in Delphi the DataType column editor opened on the first click.
Suspected files: `src/EditorTable.pas` (`ColumnGrid` `OnSelectCell` / `OnMouseDown` after the #20/#22 changes, `HideEdit` path), `lcl/grids.pas` (`goEditing`, `goAlwaysShowEditor` options of the grid in `src/EditorTable.lfm`).
Evidence: `13-multi-c.png`, `15-all.png`.

Observations (not entries): (a) after `Cancel` on the "save the model before closing?" prompt a second identical Confirmation window (new xid) appeared at the top right of the screen once; it could not be told apart from a lost first click on the modal dialog (the first click after `windowactivate` is regularly dropped), so it is not filed. (b) The main window came back at 600x426 after the restart with tips enabled (the earlier session had 1287x758); window-size persistence was not part of this pass.

## Round 10 (2026-09-09): regions, notes, images

Date: 2026-09-09, 30-minute time box, real display DISPLAY=:0, binary rebuilt with `lazbuild --build-all` from b5f3571 (branch a3), WorkMode=0 / ShowTipsOnStartup=0 during the run and restored to 2 / 1 afterwards. All work on copies under `<scratchpad>/shots/round10/` (`round8.xml` = working model, saved several times; `order.xml` copied but never opened; `test.png` / `test.bmp` = 120x80 orange squares from `convert`; driver `h.sh`; stderr `run.log`: only the canberra line, no exception, no GLib/GTK critical). `git status` clean apart from this file. Coordinates: the client origin of the main window was `xwininfo` "Absolute upper-left" (42,69), 37 px below the frame - the first 20 minutes of drags used the frame position and hit the wrong toolbar buttons (`01`-`05` shots are void).

### 35. Region tool: a rubber-band drag with the Region tool creates nothing (no region, no undo entry, nothing in the XML)

Severity: functional (regions cannot be created in the port; they only exist when loaded from an existing file).
Steps: `round8.xml`; click the grid-icon "Region" tool button in the main-window tool bar (`wtRegionSBtn`, verified Down in `26-tb0.png` / `26-all.png` left and in `08-all.png` left); press the left button on empty canvas left of `parent` (screen 322,308), move in four steps to (472,523) so that the rectangle encloses `parent`, release. Same with an empty area lower on the canvas (642,673 -> 802,773, `19-*`). Ctrl+S.
Observed: while the button is held a dotted selection rectangle is shown (`08-all.png` middle / `08-mid-z.png`: only its top and left edges are visible, see observation (b)); after the release the rectangle disappears, no region is drawn (`26-all.png` right, `19-all.png`, `08-all.png` right), the tool bar falls back to the Pointer tool (`27-chk.png` left: Pointer Down again), Edit shows a bare `Undo` (`09-edit-c.png`; `19-all.png` top shows `Undo New Object` but that is the image of #37), `grep "<REGION "` on the saved `round8.xml` finds nothing (four attempts, two with the tool state verified). The Image tool with the very same drag mechanics creates its object (`17-c.png`, `Image_02` 60x200 = the drag size), so drag delivery is not the problem.
Expected: a coloured region of the dragged size behind `parent`, `Undo New Object`, a `<REGION ...>` record on save.
What the evidence says about the cause: the fall-back to the Pointer tool proves that `TEERModel.DoMouseUp` (`src/EERModel.pas:1617`) took its `wtRegion` branch (`DMEER.SetWorkTool(wtPointer)` at :1626 is only reached there) and therefore called `NewRegion(ReEvalZoomFac(SelectionRect.Left), ..., True)`. `TEERModel.NewRegion` (`src/EERModel.pas:2794`) has exactly one silent exit, `if(w<20)or(h<20)then Exit` (:2798), before the object is created and logged - `NewImage` (:2844) has the same guard commented out, which is why the image works. Not confirmed: whether `ReEvalZoomFac(SelectionRect.Width/Height)` is really < 20 at that moment (the rect looked full size mid-drag, and `Image_02` received 60x200 from the same values) or whether `TEERRegion.Create` / `RefreshObj` fails in a way that AppException does not show. A `writeln` of `w,h` in `NewRegion` is the first thing to try. Last attempt of the run (`32-all.png`: Region button state ambiguous, drag around `child`, `child` still selected afterwards, bare `Undo`) is not counted as evidence either way.
Evidence: `26-tb0.png`, `26-all.png`, `27-chk.png`, `19-all.png`, `08-all.png`, `08-mid-z.png`, `09-edit-c.png`, `round8.xml` (no REGION element).
Suspected files: `src/EERModel.pas:1617-1627` (`DoMouseUp` wtRegion branch), `:2794-2840` (`NewRegion`), `:1533-1560` (`DoMouseMove` selection-rect update), `:2542` (`SetSelectionRectPos`).
Status: not reproduced - the Region tool creates its region; the round-10 drags ran with the Pointer tool. `writeln` probes in `TDMEER.SetWorkTool`, `DoMouseDown/Move/Up` and `NewRegion` (`fix35/run.log`): a click that really hits `wtRegionSBtn` logs `SetWorkTool 7`, the drag then logs `up tool=7 vis=TRUE L=242 T=204 W=120 H=210`, `NewRegion x=242 y=204 w=120 h=210 colors=6`, and `Region_01` is drawn (`fix35/1112.png` right). Clicks 20-30 px off the button (my own first four attempts at the y taken from a screenshot crop) log nothing and the drag runs with the Pointer tool, which gives exactly the round-10 picture: rubber band, nothing created, bare `Undo`, Pointer Down `again` (it never left). The tool-bar buttons are at `Top` + ~60 px of the window (the Design/Query images sit above them); the crops of the Down state are not reliable, check `SetWorkTool` or the status-bar text instead. What was a real bug is observation (b): `DoSelectionRectPaint` and `DoGridPaintBoxPaint` used `width`/`height` inside `with <paintbox>.Canvas do` - in the LCL `TCanvas` (via `TFPCustomCanvas`) has its own `Width`/`Height` (the device size of the parent's DC), CLX had none, so the right and bottom edges were drawn outside the paintbox and clipped; both now use the paintbox size (`src/EERModel.pas` `DoSelectionRectPaint`, `DoGridPaintBoxPaint`). Verified on the real display: full four-edge rubber band with the Region tool (`fix35/1112.png` left) and with the Pointer tool around `child`, which is selected on release (`fix35/1415.png`); `parent` inside the region stays clickable (`fix35/131617.png` left); Ctrl+Z removes the region, Ctrl+Y brings it back (`131617.png` middle/right - with the next region colour, yellow instead of red, because the redo goes through `NewRegion` and advances `LastRegionColor`; the original does the same); Ctrl+S writes `<REGION ID="1300" RegionName="Region_01" XPos="242" YPos="204" Width="120" Height="210" RegionColor="1" ...>`; a double-click on the region opens the Region Editor (`fix35/18-editor-s.png`: name, colour, database / table type with overwrite boxes, comments, OK/Cancel; Escape closes it - not exercised further). See notes-to-myself.md "Fix: model-edit #35".

### 36. Pointer-tool rubber band around a note does not select it (unconfirmed)

Severity: functional if real; confidence low (one attempt).
Steps: Pointer tool; drag from (625,455) to (745,505) around the two-line note placed in #38 (note drawn at about image x 600-700, y 400-420, client origin 42,69); open Edit.
Observed: `Copy selected Object(s)` disabled, i.e. nothing selected (`20-edit-c.png`); a single click on the note does select it (Ctrl+Del then asks "Confirmation", `21-both.png`).
Expected: the note selected, Copy/Cut/Delete enabled. Follow-up in the same run: the same rubber band around the `child` table does select it (`31-all.png`: Copy enabled, dotted outline), so the pointer rubber band works for tables and the miss is note-specific or my rectangle. Possibly the same selection-rect size problem as #35 (`SelectObjsInSelectionRect` is called from the `wtPointer` branch of `DoMouseUp` :1609 with the same rectangle); could also be my rectangle missing the note by a few pixels - needs a retry with a generous rectangle around a table.
Evidence: `20-edit-c.png`.
Suspected files: `src/EERModel.pas:1609-1615`, `SelectObjsInSelectionRect`.
Status: not a bug - the press did not start a rubber band at all. `writeln(stderr)` probes in every `DoMouseDown` (`fix36/run.log`): the round-10b press at screen (415,385) is caught by `Rel_02_RelMiddle`, the invisible `RelIconSize+1` (about 15 px) wide `TPaintBox` that follows the middle segment of the `OnlineorderRel` relation line and makes the 1-px line clickable at all (`TEERRel.RefreshObj`, same geometry and handler as the original). `TEERRel.DoMouseDown` then deselects everything, selects the relation and the drag moves its middle segment (`MidOffset`); the "dotted band" of `12m-c.png` is the selected relation's frame around its label (`fix36/06-c.png` shows the same after the replay: `OnlineorderRel` framed, no band, note unselected). A band that really starts on the region body works: press at screen (300,470) -> `region down OnlineStore X=209 Y=301`, release -> `SelectObjsInSelectionRect rect=222,289 220x85` (control pixels at zoom 75 %, model (296,385)-(589,499)), `Note_03` (486,406 102x41) gets the dotted frame together with `onlineorderhasproduct`, and Ctrl+C / Ctrl+V pastes both (`fix36/04-c.png`, `05-c.png`). Note for the coordinates in the round-10b entry: `order.xml` opens at zoom 75 %, so the screen-to-model conversion there was off by the zoom factor and the canvas origin (window client (36,27)); model = (screen - (78,96)) / 0.75 for that window. No code change. What would make it go away is a line-distance hit test in the relation paintboxes instead of the rectangle - a change of the original's behaviour, left open. See notes-to-myself.md "Fix: model-edit #36".

### 37. Image placed with a drag size different from the picture size is not drawn after save/reopen

Severity: functional (the object is in the file but invisible; the first image, whose drag size equalled the picture size, survives).
Steps: Image tool; drag 120x80 at (642,553)-(762,633), pick `test.png` in the "Open an Image ..." dialog (filter `PNG files|Bitmap files|All files`, `src/EERModel.pas:13893`), the orange square appears (`17-c.png`); Image tool again, drag 60x200 at (900,300)-(960,500), pick `test.bmp`; Ctrl+S; `pkill`, relaunch with `round8.xml`.
Observed: XML has `Image_01 ... Width="120" Height="80" StrechImg="1" ImgWidth="120" ImgHeight="80" ImgFormat="BMP" ImgData=424D...` and `Image_02 ... XPos="820" YPos="200" Width="60" Height="200" StrechImg="1" ImgWidth="120" ImgHeight="80"` with byte-identical `ImgData` (57935 chars each, PNG converted to BMP hex as in the original). After reopen `Image_01` is drawn (`24-c.png`) but the area of `Image_02` is blank (`24-img2.png`, `28-c.png` - crop of model 820..1120 x 200..520 is white). Whether `Image_02` was drawn before the reopen was not captured (the second placement was only checked in the XML).
Expected: both images drawn, the second stretched to 60x200 (`StrechImg="1"`).
Evidence: `round8.xml`, `24-c.png`, `24-img2.png`, `28-c.png`, `17-c.png`.
Suspected files: `src/EERModel.pas:13768` (`TEERImage.PaintObj2Canvas`), `:13833` (`RefreshObj`), `:13905-13925` (`LoadImageFromFile`, `if(Obj_W<2)then Obj_W:=Img.Width` and the `StrechImg` branch), the IMAGE loader around `:3760`.
Status: fixed in the loader, not in the paint path - the drag size was a red herring. The two `ImgData` blobs in `round8.xml` are the same length but not byte-identical: `Image_02`'s pixel area is all `FF` (decode both with `bytes.fromhex`), i.e. the picture was already white when it was placed and saved, and reopening drew that white bitmap faithfully (`writeln` probes: `Img.Canvas.Pixels` of `Image_02` = `FFFFFF` right after `TPicture.LoadFromStream`, `Image_01` = `00A5FF`). The difference between the two placements was the file, not the drag: `Image_01` came from `test.png` (1-bit colormap PNG, decoded by the PNG reader into a 32-bit `TBitmap`), `Image_02` from `test.bmp`, a 1-bpp BMP (palette entry 0 = orange, all bits 0). Under the LCL/GTK2 a `TBitmap` loaded from a 1-bpp BMP gets `PixelFormat = pf1bit` and a monochrome handle whose palette is lost - every pixel reads back white (`fix37/t/t.lpr`: `p.Bitmap.Canvas.Pixels[60,40] = FFFFFF`, `TLazIntfImage` created from the handle also white; a `TLazIntfImage` with an RGB description or a plain `TFPMemoryImage` reading the same file gives `FFA500`). Fix: `TEERImage.LoadImgFromStream` (`src/EERModel.pas`) loads the stream into a `TLazIntfImage([riqfRGB])` and `Img.LoadFromIntfImage`, used by `LoadImageFromFile`, `SetXML` and `SetXML2`; the XML format is unchanged (`Img.SaveToStream` still writes the same 24-bit BMP, 28854 bytes for 120x80). Verified on the real display: `round8b.xml` (copy of `round8.xml`) + Image tool drag 60x200 with `test.bmp` -> orange stretched bar (`fix37/04-c.png`), Ctrl+S writes `Image_03 ... Width="60" Height="200" ... ImgData` with orange pixels, reopen draws it (`fix37/05-after-c.png`), Size tool drag enlarges it to 120x250 re-stretched (`fix37/07-resize-c.png`); the `order.xml` copy draws both PNG images at the top-left (`fix37/08-order-c.png`) and a double-click on the fabFORCE logo opens the "Image Editor" (`fix37/09-imgeditor.png`, 459x122; Escape closes it) - the round-5 double-click problem did not reproduce here. Not fixable: `Image_02` in the round-10 `round8.xml` stays white, the white pixels are in the file. See notes-to-myself.md "Fix: model-edit #37".

### 38. (verified OK, for the record) Note create / edit / two-line non-ASCII text / delete / undo / save / reopen

Steps: Note tool, click on the canvas -> note "Doubleclick on the Note to edit the Text." (`10-all.png`); double-click -> modeless "Note Editor" (298x186: `Note Text` label, memo with the text preselected, check / cross buttons; no font or colour controls - the original `EditorNote.xfm` has none either; the memo shows a permanent vertical scrollbar, cf. #21) (`11-noteed.png`); Ctrl+A, type `Größe der Tabelle` Return `zweite Zeile: Ärger` (xdotool delivered `ärger`, a driver artefact), click the check button -> canvas shows both lines with the umlauts (`15-c.png`, `17-c.png`); single click, Ctrl+Del -> Confirmation, Return -> note gone; Ctrl+Z -> note back and selected (`21-both.png`); Ctrl+S -> `NoteText="Gr\195\182\195\159e der Tabelle\nzweite Zeile: \195\164rger\n"` (UTF-8 bytes escaped one by one, the original escaped Latin-1 bytes the same way); relaunch -> both lines with umlauts intact (`24-c.png`).
Also: Ctrl+S while the Note Editor was open saved the model (`13-*` step, file mtime 17:44:49) - consistent with the editor being modeless (`src/Main.pas:2130` `EditorNoteForm.Show`), same as the original; not filed as a bug.

Observations (not entries): (a) `src/Main.pas:2615` `wtNote: wtRegionSBtn.Down:=True` in `DisplaySelectedWorkTool` - selecting the Note tool presses the Region button, inherited verbatim from the original (`Main.pas` of the first commit, same lines); the tool bar in `10-all.png` shows neither Note nor Region visibly Down, so cosmetic at most. (b) The rubber-band rectangle is drawn with only its top and left edges (`08-mid-z.png`); `DoSelectionRectPaint` (`src/EERModel.pas:2494`) draws all four with `LineTo(width-1, ...)`, so either the paintbox is clipped or its size differs from the painted one - possibly the same root as #35/#36, not investigated. (c) Image "Open an Image ..." dialog accepts PNG and BMP only (no JPEG filter), and the PNG is re-encoded as BMP hex in the XML - same as the original.

Exercised / not reached (round 10): exercised - Region tool selection and four drags (#35); Note tool placement, Note Editor, two-line umlaut text, canvas rendering, single-click select, Ctrl+Del + Confirmation, Ctrl+Z, Ctrl+S, XML encoding, reopen (#38); Image tool with PNG and BMP, the open dialog and its filter, XML storage format, reopen (#37); pointer rubber band once (#36). Not reached - Region Editor (name / colour / comments), region move / resize / z-order / delete / undo / save (no region could be created); note resize and move; image double-click editor, resize (aspect), move, delete + undo; the two images in `order.xml` (never opened); step 4 entirely (multi-select copy/paste/delete/undo of mixed objects, Export Model as Image, Export selected Objects as Image, Copy as Image / clipboard targets); step 5 (Navigator thumbnail, Display options).

### Round 10b (2026-09-09): regions, images, mixed selections, exports

Second pass, 30-minute time box, binary rebuilt with `lazbuild --build-all` from 5c8595a (branch a3), same display rules as round 10 (WorkMode=0 / ShowTipsOnStartup=0 during the run, restored afterwards). All work on `<scratchpad>/shots/round10b/order.xml`, a copy of `bin/Examples/order.xml` (zoom 75 %, three regions, three notes, two images, 14 tables); `order-before-mixed.xml` / `order-pasted.xml` are the snapshots around step 4; driver `h.sh` (copy of `fix35/h.sh`); `run.log` = only the canberra line, no exception text, no GTK critical (the exception of #39 is shown as a dialog only); `gdb.log` = the backtrace of #39. `git status` clean apart from this file. Screen = (77 + 0.75*model_x, 100 + 0.75*model_y) for this window (client origin 42,69, canvas origin 35,31 in the client).

### 39. Export Model as Image: "Division by zero" exception dialog after every export (the file itself is complete)

Severity: functional (an "Division by zero. Press OK to ignore and risk data corruption / Press Abort to kill the program" dialog after each image export; the PNG is written and correct, so the export is usable but looks broken).
Steps: `order.xml` copy; File > Export > Export Model as Image ...; in the "Save Model As ..." dialog Ctrl+A, type `<scratch>/export-model.png`, Return.
Observed: `export-model.png` (990x900) is written and shows all three regions with their colours, the three notes, both images, all tables and relations (`export-model-s.png`), then the exception dialog `24-msg.png` appears; OK dismisses it, the application keeps running; second export under gdb (`export-model2.png`) identical.
Expected: no dialog.
Cause (confirmed by the gdb backtrace in `gdb.log`, `handle SIGFPE stop`): the "Division by zero" is not an arithmetic error but the LCL's `RaiseGDBException` (laztracer.pas:58, which raises SIGFPE on purpose) with the message `TGtk2WidgetSet.DeleteObject invalid GdiObject=...`, called from `gtk2winapi.inc:1917 DeleteObject` <- `TSharedRasterImage.FreeHandle` <- `TRasterImage.Destroy` <- `ModelBmp.Free` at `src/Main.pas:1254` (the `finally` of `SaveModelasImageMIClick`, right after `DMMain.SaveBitmap(ModelBmp.Handle, ...)` at :1252). So the bitmap handle of `ModelBmp` is already destroyed when `ModelBmp` is freed. `TDMMain.SaveBitmap` (`src/MainDM.pas:1707-1745`) wraps the caller's handle in a second `TBitmap` (`Bmp.Handle := Handle`, then `Png.Assign(Bmp)`, then `Bmp.Handle := 0; Bmp.Free` with the comment "Don't free the handle - caller owns it"). Which of these steps deletes the handle was not verified (`TRasterImage.SetHandle` only calls `UnshareImage`, which exits for RefCount <= 1, so the `Handle := 0` itself does not delete; `Png.Assign(Bmp)` / `RawImageNeeded` are the remaining candidates). Whatever the step, sharing one GDI handle between two `TBitmap`s is the pattern to remove: pass the `TBitmap` (or a `TRasterImage`) to `SaveBitmap` instead of its handle, or `Assign` the PNG straight from `ModelBmp`. Export selected Objects as Image (same handler, `Tag=2`) with the mixed selection of #40 writes `export-sel.png` (590x900: region `OnlineStore`, both images, notes, the selected tables) and shows the same dialog (`export-sel-s.png`); `Copy selected Object(s) as Image` (`src/Main.pas:3715`) was not run (no `xclip` on this machine anyway).
Evidence: `24-msg.png`, `export-model-s.png`, `gdb.log`, `gdb.cmd`.
Suspected files: `src/MainDM.pas:1707-1745` (`SaveBitmap`), `src/Main.pas:1246-1256`.
Status: fixed - the handle sharing was the whole story, no arithmetic error behind the "Division by zero". `TDMMain.SaveBitmap` (`src/MainDM.pas`) now takes the `TBitmap` itself under FPC (`QPixmapH` kept for the Delphi/CLX branch) and assigns the PNG / JPEG straight from it (`Png.Assign(Bmp)`, `Jpg.Assign(Bmp)`, `Bmp.SaveToFile` for BMP); the second `TBitmap` wrapper with `Bmp.Handle := Handle` / `Bmp.Handle := 0` is gone. Under the LCL `TRasterImage.SetHandle` hands the handle to the wrapper's `TSharedRasterImage`, and `Png.Assign` -> `RawImageNeeded` / `Handle := 0` re-creates the image, so the original handle was deleted by the wrapper and `ModelBmp.Free` deleted it a second time. `SaveModelasImageMIClick` (`src/Main.pas`) passes `ModelBmp` (`ModelBmp.Handle` for Delphi); `CopyselectedObjectsasImageMIClick` never shared the handle (`Clipboard.Assign(ModelBmp)`) and is unchanged. Verified on DISPLAY=:0 (`fix39/`, `order.xml` copy): Export Model as Image to `model.png` - no dialog, `identify` 990x900 PNG, the three regions with their colours, both images, the notes, all tables and relations visible (`model-small.png`); second export in the same session to `model2.jpg` - no dialog, 990x900 JPEG; News + Employee selected (shift-click, `sel-c2.png`), Edit > Copy selected Object(s) as Image - no dialog, status bar "Selected Object(s) copied to clipboard as image." (`status-c.png`); Export selected Objects as Image to `sel.png` (overwrite prompt answered Yes) - no dialog, 730x860 PNG with the two tables at the top-left of a mostly empty canvas (`sel-small.png`; the selection export keeps the model origin instead of cropping to the selection - same as round 10b's 590x900 `export-sel.png`, inherited `PaintModelToImage` bounds, not changed here); `run.log` = only the canberra line. See notes-to-myself.md "Fix: model-edit #39".

### 40. Paste of a mixed selection renames the tables but not the regions, notes and images (duplicate names and duplicate OrderPos)

Severity: low/functional (duplicate object names; every region/note/image of the pasted copy has the same `OrderPos` as its original, so the z-order of the copies is undefined).
Steps: Pointer tool; rubber band from screen (80,103) to (532,792), i.e. model (4,4)-(606,922), which encloses region `OnlineStore`, its 7 tables, `Note_03`, `Note_05`, `Image_03`, `Image_04`; Ctrl+C; Ctrl+V; Ctrl+S.
Observed (`order-pasted.xml`): 21 tables, 5 notes, 4 images, 4 regions, 20 relations - all object types are pasted, all at +30/+30 (region 17,97 -> 77,127; Note_03 486,406 -> 516,436; Image_03 12,16 -> 42,46) and the copies of the tables are renamed `product_1`, `onlineorder_1`, ...; but the pasted region is again `OnlineStore` (`OrderPos="3"` like the original), the notes again `Note_03` / `Note_05` (`OrderPos` 33/34), the images again `Image_03` / `Image_04` (`OrderPos` 35/36). Ctrl+Z (`Undo Paste Object(s)`) removes all pasted objects (counts back to 14/3/2/3/12), no leftovers (`18-c.png` = state after paste).
Expected: unique names for all pasted objects (`Note_06`, `Image_05`, `OnlineStore_1`) and fresh `OrderPos` values, as `NewNote` / `NewImage` / `NewRegion` assign for newly created objects.
What the code says: `src/Main.pas:1479-1500` (`PasteMIClick`) renames only `TEERTable` instances ("give tables that collide with an existing table a unique name") - the pasted objects are loaded by `LoadFromFile(..., Select)` with their saved names and `OrderPos`. Whether the original DBDesigner 4 renamed notes/regions on paste was not checked.
Evidence: `order-before-mixed.xml`, `order-pasted.xml`, `17-c.png` (selection), `18-c.png`.
Suspected files: `src/Main.pas:1426-1530`, `src/EERModel.pas` `LoadFromFile` (OrderPos handling on paste).
Status: fixed - `PasteMIClick` (`src/Main.pas`) now runs the unique-name loop over every pasted `TEERTable`, `TEERRegion`, `TEERNote` and `TEERImage` (`NameUsedByOther` compares against objects of the same class, same `_1`, `_2` scheme; relations are left alone) and gives each pasted object a fresh `OrderPos` (largest `OrderPos` of the non-pasted objects + 1, +2, ... in component order); the paste log (`at_PasteObj` sub actions) is still written after the rename, so undo/redo carry the final names. `OrderPos` is not the z-order: it is only the sort key of the DB Model palette tree (`SortEERObjectListByOrderPos`, drag reorder in `src/PaletteModel.pas`) and a saved attribute, so no repaint order changes. The original Delphi `PasteMIClick` (8b2b15f) renamed nothing at all - the table rename is the port's (#26-#28), now extended. Verified on DISPLAY=:0 (`fix40/`, same band (80,103)-(532,792), Ctrl+C, Ctrl+V, Ctrl+S): `after-paste.xml` = 21/4/5/4 with `OnlineStore_1` (OrderPos 37), `product_1` ... `creditcard_1` (38-44), `Note_03_1`/`Note_05_1` (45/46), `Image_03_1`/`Image_04_1` (47/48), originals unchanged; Ctrl+Z, Ctrl+S: `after-undo.xml` = 14/3/3/2/11, no `_1` name left.

### 41. Image Editor: the three TBitBtn captions are clipped at the top

Severity: cosmetic.
Steps: `order.xml` copy; double-click `Image_04` (the "DB Designer 4" logo, screen 160,148).
Observed (`25-imged.png`, window 459x122): "Restore Size", "Restore Aspect Ratio", "Clear Image" are drawn with the upper third of the letters cut off ("Hestore Size"); the name edit, the folder button and "Enable Image streching" are fine. The form has no OK/Cancel buttons at all - that is by design (`src/EditorImage.lfm`: `bsToolWindow`, `ClientHeight = 122`, the three buttons at `Top = 80`, `Height = 25`, `Anchors = [.., akBottom]`; the original `EditorImage.xfm` has the same layout), so nothing is missing. Editing beyond opening the form (change picture, stretch, OK) was not reached.
Expected: full captions.
Suspected: `src/EditorImage.lfm:103-130` (TBitBtn with `Height = 25` and a glyph/layout setting that shifts the caption - not verified); compare with the Note Editor buttons which render correctly.
Evidence: `25-imged.png`.
Status: fixed - `src/EditorImage.lfm`: the three `TBitBtn`s 25 -> 30 px high (Top 82), `ClientHeight` 122 -> 130. A GTK2 button needs ~28 px for the `Sans -11` label and the LCL clips the requisition at the top when the allocation is smaller (same cause as the Options "Reset Personal Settings" button of ui-bug #16). Verified on DISPLAY=:0 (`fix40/03-imged.png`, 459x130): "Restore Size", "Restore Aspect Ratio", "Clear Image" complete, name edit / folder button / "Enable Image streching" unchanged and inside the form; closed through the WM close button (the tool window has no OK/Cancel and `FormKeyDown` handles F1 only - inherited).

### 42. Undo of a multi-object delete loses every relation whose two tables were both deleted (and their FK columns)

Severity: functional, data loss (undo is silently incomplete; the saved model lacks 8 of 12 relations and 7 FK columns).
Steps: `order.xml` copy; Pointer rubber band over the whole `OnlineStore` region as in #40 (region, 7 tables, 2 notes, 2 images); Ctrl+Del; the Confirmation dialog lists all objects incl. the relations (`26-confirm-s.png`); Yes; Ctrl+S (`T=7 N=1 I=0 R=2 REL=3`); Ctrl+Z (`Undo Delete Object(s)`); Ctrl+S.
Observed (`order-before-del.xml` vs `order-after-del-undo.xml`): tables 14 -> 7 -> 14, notes 3 -> 1 -> 3, images 2 -> 0 -> 2, regions 3 -> 2 -> 3 with identical positions and `OrderPos` - but relations 12 -> 3 -> 4. Restored: `forumpost` (onlinecustomer -> forumpost, only one end deleted). Lost: `CartRel`, `CreditCardRel`, `CustOrderRel`, `OnlineorderRel` (x2), `ProductgroupRel`, `ProductInCartRel`, `ProductRel` - exactly the relations whose source and destination tables were both in the selection. `IsForeignKey="1"` columns 11 -> 4, i.e. the FK columns those relations had added are gone as well; no message, `run.log` empty. `28-c.png` = canvas after the undo (tables back, connectors missing).
Expected: undo restores the model byte-for-byte (apart from IDs).
What the code says (mechanism visible, fix not verified): `TEERModel.DeleteSelectedObjs` (`src/EERModel.pas:5232`) calls `DeleteObj` on every selected object inside one `at_DeleteObj` sub-action log; `TEERTable.DeleteObj` (`:8055-8075`) first logs its own XML (`LogSubAction(at_DeleteObj, Obj_id, GetObjAsXMLModel)`) and *then* deletes its relations (`TEERRel(RelStart[0]).DeleteObj`, which log themselves). The undo of `at_DeleteObj` (`:5596-5612`) walks the sub-actions backwards and does one `LoadFromFile(undo.xml)` per entry, so a relation record is reloaded before the table that was logged *before* it exists again; a RELATION whose `SrcTable`/`DestTable` cannot be resolved is presumably dropped by `LoadFromFile` (not read). With a single table + its relations the order happens to work (table logged first, relations after, undo restores relations after the table), which is why round 8/9 single-table undo passed. A fix must either log all tables before any relation (two passes in `DeleteSelectedObjs`), or restore in two passes (objects, then relations), or write one combined undo XML.
Evidence: `order-before-del.xml`, `order-after-del-undo.xml`, `26-confirm-s.png`, `27-c.png` (after delete), `28-c.png` (after undo).
Suspected files: `src/EERModel.pas:5232-5257` (`DeleteSelectedObjs`), `:8055-8075` (`TEERTable.DeleteObj`), `:5596-5612` (undo `at_DeleteObj`), `LoadFromFile` relation resolution.
Status: fixed - cause confirmed with `writeln` probes in `LogSubAction` / `GetRELATIONS` (`fix42/run.log`): `DeleteSelectedObjs` walks `Components` downwards, so the 7 *selected* relations (highest component indices) were logged and deleted first, then the tables, and `GetRELATIONS` skipped all 7 on undo (`skip rel 1179 src=1174 dst=414` ...); `UndoActions` replays the sub actions forwards (not backwards as the entry guessed - `RedoActions` is the one walking backwards). Deleting a relation first also let `CheckAllRelations` strip the FK columns out of the still-alive destination tables before those were logged, which is where the FK columns went. `DeleteSelectedObjs` now writes the whole log itself before deleting anything - tables first (FK columns intact), then notes/images/regions, then every relation involved (selected ones plus those hanging on the selected tables, deduplicated) - and deletes with `LogActions` off; `TEERTable/TEERRel.DeleteObj` keep logging themselves for the single-object popup delete. Same ordering in the original Delphi source (8b2b15f), so inherited. Verified on DISPLAY=:0 (`fix42/`, `order.xml` copy, same band (80,103)-(532,792)): counts orig `T=14 REL=11 FK=11 N=3 I=2 R=3` -> Ctrl+Del `T=7 REL=3 FK=3 N=1 I=0 R=2` -> Ctrl+Z `14/11/11/3/2/3` (`f-after-undo.xml`) -> Ctrl+Y `7/3/3/1/0/2`, byte-identical METADATA to the first delete (`diff-f-after-del-vs-f-after-redo.txt`, 0 lines) -> Ctrl+Z `14/11/11/3/2/3` again; the sorted TABLE/COLUMN/INDEX/RELATION/NOTE/IMAGE/REGION lines of the undone model equal the original except (a) the FK column `idonlinecustomer` of the surviving `forumpost` table comes back with a new id (its relation was deleted, the column stripped and recreated - the same "ids may differ" behaviour as the single-table undo), (b) the two images come back as `ImgFormat="BMP"` with a larger `ImgData` (re-encoded from memory; position/size/`OrderPos` identical - separate observation, not fixed here). Single-table case (`onlinecustomer` click + Ctrl+Del + Ctrl+Z): `13/7/7` -> `14/11/11`, four connectors back (`2223.png`), FK columns in the surviving `onlineorder`/`cart` recreated with new ids as in round 9b. See notes-to-myself.md "Fix: model-edit #42".

Resolution of #36 (rubber band does not select a note): confirmed, but position dependent. A band from screen (415,385) to (545,460) - model (450,380)-(624,480), which encloses `Note_03` at model (486,406) with a margin of 35 px on every side - shows the dotted rubber band while dragging (`12m-c.png`) and leaves the note unselected after the release (`12-c.png`: no dotted frame; a single click on the same note gives the dotted frame, `14-c.png`); Ctrl+C / Ctrl+V after that band adds nothing to the model (counts unchanged after Ctrl+S) although Edit afterwards shows `Undo Paste Object(s)` (`15-c.png` / `16-c.png`; Edit > Copy is enabled in both cases, so the menu is no indicator). The large band of #40, started at model (4,4) outside every region, did select `Note_03` and `Note_05` (both were pasted). The failing band started inside the body of region `OnlineStore`; the working band started outside all regions and enclosed the region as a whole. `SelectObjsInSelectionRect` (`src/EERModel.pas:2585-2604`) compares the rect with `Obj_X/Y/W/H`, and `TEERNote.RefreshObj` (`:12744-12762`) does set `Obj_W/Obj_H` from the text extent, so the enclosure test itself looks fine; what differs when the drag starts over a region (mouse-down hit on the region, `DoMouseDown` / `DoMouseUp` paths) was not read. Next step: `writeln` of the rect and of each candidate's `Obj_*` in `SelectObjsInSelectionRect`, plus the `DoMouseDown` branch taken when the press lands on a region body.

Round 10b OK (not entries): Region Editor (`01-regioned.png`): all controls readable, colour combo drops down with Red/Yellow/Green/Cyan/Blue/Magenta (`03-dropdown.png`), name / colour / comment reach the canvas and the XML after OK (`RegionName="SysTables2" RegionColor="4" Comments="... EDITED"`); note that the colour was already in the saved XML while the editor was still open (Ctrl+S with the modeless editor open), i.e. the colour combo applies immediately, name and comment only on OK - not compared with the original. Region move by its title bar (+200 model px): the three tables and `Note_02` inside move with it (`08-c.png`, XML), Ctrl+Z puts region, tables and note back (XML identical). Export Model as Image renders every object type (see #39). The image export dialog is titled "Save Model As ..." (`src/Main.pas:1222`, translated message 12) - cosmetic, probably inherited.

Proposed grouping for this pass: (A) undo/paste bookkeeping of multi-object actions - #42 (relations lost on undo) and #40 (names/OrderPos on paste), both in the `LoadFromFile`-based replay; (B) image export handle sharing - #39; (C) selection - #36; (D) cosmetic - #41. Recommended order: #42, #39, #36, #40, #41.

Exercised / not reached (round 10b): exercised - Region Editor open/edit/OK and XML, region move with contents + undo, pointer rubber band around a note (twice) and around a whole region, mixed copy/paste + undo, Ctrl+Del of the mixed selection with Confirmation + undo (XML diff, #42), Export Model as Image (twice, once under gdb), Export selected Objects as Image, Image Editor opened. Not reached - Region Editor Cancel; region resize; z-order (click on a table inside a region, region popup menu); region-only Ctrl+Del + undo/redo; note move/resize and persistence; Image Editor change picture / Stretch / OK; image move, delete + undo; redo after the #42 undo; Copy selected Object(s) as Image / clipboard targets (no `xclip` installed); Navigator thumbnail; Display menu toggles.

## Round 11 (2026-09-09): query mode against MySQL and SQLite

30-minute time box, binary rebuilt with `lazbuild --build-all` from b3da86b (branch a3). Display rules as in round 10 (WorkMode=2 / ShowTipsOnStartup=0 during the run, settings and `DBConn.ini` restored afterwards; the stored connection `OrderMySQL` pointed at a scratch database `dbdround11` built with `tests/mysql-roundtrip.sh` from `order_mysql.sql`, an earlier MySQL export of `order.xml`; `OrderSQLite` pointed at `order.sqlite`, a copy of the round-8 verification database; `dbdround11` dropped at the end). All work on `<scratchpad>/shots/round11/order.xml`, a copy of `bin/Examples/order.xml`; drivers `h.sh` (round-10 helpers) and `q.sh` (type a statement into the SQL memo, execute, capture grid + status bar). `run.log` = only the canberra line for the whole run (both launches), no exception text. `git status` clean apart from this file. Screen = client (42,69) + control position; SQL memo click point (442,689), Execute button (1814,675), Store SQL button (1814,723), grid rows at screen y 841/861/881.

### Stage table

| Stage | MySQL 8.0 (`dbdround11`) | SQLite (`order.sqlite`) |
|---|---|---|
| 1 Query mode layout | works: palette SELECT/FROM/WHERE/GROUP/HAVING/ORDER/SET buttons, stored-SQL tree (SQL Commands / Scripts / Table Selects / History), SQL memo, grid, two button columns all readable, no clipped captions (`01-main-s.png`, `01-palette-c.png`) | same window |
| 2 Connect + build + execute | works: Connect dialog lists both connections (`03-connsel-s.png`), status "Connected to Database bpsa@dbdround11"; dragging a table shows the "Query Drag Target" form (SELECT / Add Table(s) / JOIN Table(s) / LEFT OUTER JOIN / UPDATE / INSERT / DELETE, `07-dt-4200045.png`), drop on SELECT writes `SELECT *\nFROM product product`; Execute -> 3 rows, 7 columns, "Query opened. 3 Record(s) fetched. Time: 00:00:030" (`09-combo.png`, `09-bl-c.png`); JOIN + WHERE string literal + ORDER BY DESC correct (`10-join-c.png`), zero rows -> "0 Record(s)" but see #46, syntax error -> error box with the server message, app continues (`16-dlg-4205327.png`); NULL TEXT/BLOB columns empty; DECIMAL 14.20 shown as `14.2`; DATETIME/DATE see #45; first cell see #44; one bogus "1 Record(s)" result see #47 | works the same way: 3 rows, JOIN/WHERE/ORDER correct, 0 rows (#46), error box `TSQLite3Connection : near "SELEC": syntax error`-style dialog (`54-dlg-s.png`), same #44/#45 (`55-combo.png`) |
| 3 Result grid | click selects a row (indicator moves), Down key / typing do not edit (#43); right-click popup Copy to Clipboard / Export Records / Print Records to PDF present (`29-combo.png`); Ctrl+C not verifiable (no `xclip`/`xsel` on this machine); column resize / header sort not reached | not repeated |
| 4 Stored SQL | Store SQL button -> "Name:" dialog, `Round11/prod` creates folder + node (`32-combo.png`, `34-combo.png`); rename via the node's inline dialog works in the tree (`39-combo.png`; dialog title see #49); persisted by File > Save as `<QUERYRECORD SQLCmdType="1" StoredPosition="Round11/prod" ...>` together with the 6 History entries of the session (`order.xml`); Ctrl+S does not save (#48); popup Execute / Delete Item(s): not confirmed (#50); reopened model shows the node again (`62-combo.png`) | not repeated |
| 5 SQL editor | Ctrl+A + typing replaces the text (all `q.sh` runs); plain TMemo, no syntax highlighting; multi-statement / paste / history navigation not reached | not repeated |
| 6 Mode switch | Display > Design Mode shows the design palettes, Display > Query Mode restores tree state, memo and grid, no exception (`48-combo.png`); Disconnect -> "Not connected to a Database"; connect to `OrderSQLite` works | (this is the other backend) |

### 43. Result grid: a selected cell cannot be edited and "Apply Changes to DB" does nothing

Severity: functional (no live editing of the result; no message either).
Backend: MySQL (SQLite not repeated, same code path).
Steps: `SELECT * FROM product`, Execute; click the `name` cell of row 2 (screen 480,861) twice; End; type `Z`; click the "Apply Changes to DB" button (screen 1892,699).
Observed (`28-combo.png`): the indicator moves to row 2 but no inplace editor opens and the `Z` is neither shown in the cell nor in the SQL memo (`27-memo-c.png`); after "Apply Changes" the indicator jumps back to row 1, the grid is unchanged, `SELECT name FROM product WHERE idproduct=2` in the CLI still returns `Lord of the Rings - Part I`; `run.log` empty. Whether the "Discard Changes" button or a Post via cursor movement behaves differently was not tried.
Expected (original DBDesigner 4): the grid is editable (`dgEditing` in `src/EditorQuery.lfm` DBGrid options) and `SubmitBtnClick` (`src/EditorQuery.pas:603`) posts the changes with `OutputClientDataSet.ApplyUpdates(-1)`.
What the code says (not verified): `OutputClientDataSet` is the port's `TClientDataSet` shim (`src/clx_shims/dbclient.pas`, a `TBufDataset` filled by `CopyFromDataset` from `OutputQry`, `:91-99`); the shim publishes `ReadOnly` and toggles it around the copy. Whether the buffer is left read-only, whether the LCL grid refuses editing because `DefaultDrawing=False` / `OnDrawColumnCell`, or whether the key never reaches the grid, was not determined; a `TBufDataset.ApplyUpdates` without update SQL would also need `OutputDataSetProviderUpdateError`-style handling. Next step: `writeln` of `OutputClientDataSet.CanModify` / `ReadOnly` / `State` after Open and in `DBGrid.OnKeyPress`.
Evidence: `27-edit-c.png`, `28-combo.png`, `27-memo-c.png`.
Suspected files: `src/clx_shims/dbclient.pas`, `src/EditorQuery.pas:603-608` (`SubmitBtnClick`), `src/EditorQuery.lfm` (DBGrid `Options`, `ReadOnly` not set).
Status: fixed - two causes. (1) The grid could not be edited because of `dgAlwaysShowEditor` (see #44): the always-shown LCL editor was empty and swallowed the keys; without it F2, a second click, or typing a character opens the inplace editor with the value (verified `fix43/24-29-stack.png`). (2) `ApplyUpdates` never reached the database: the port's `TClientDataSet` is a `TBufDataset` whose `ApplyRecUpdate` only raises "not supported", and worse, `CopyFromDataset` had left every fetched row in the change log as a pending *insert* (first apply attempt: `Duplicate entry '1' for key 'product.PRIMARY'`, `fix43/14-err-c.png`). `src/clx_shims/dbclient.pas` now calls `MergeChangeLog` after the copy, copies the source `TSQLQuery`'s `ProviderFlags` (`pfInKey` from `UsePrimaryKeyAsKey`) and table name, and implements `ApplyRecUpdate` as parameterised `UPDATE ... WHERE <pk>=:OLD_pk` / `INSERT` / `DELETE` statements on the source connection with `CommitRetaining` (like the Delphi `TDataSetProvider`; a JOIN or a table without primary key gives a clear "Cannot apply changes" error). `SubmitBtnClick` posts a pending edit, uses `ApplyUpdates(0)` inside a try/except with an error dialog and reports "Changes applied. N pending change(s)." / "No changes to apply." in the status bar. Note: round 11 clicked (1892,699), which is `CancelBtn` ("Discard Changes", Top=33); the Apply button is at (1892,667) - that is why the indicator jumped back to row 1. Verified on DISPLAY=:0 (`fix43/`): MySQL `product` row 2 name -> `ZZ` via double click + typing + Apply, `mysql` CLI returns `2 ZZ` (`51-52-stack.png`); SQLite row 2 -> `YY`, `sqlite3` returns `2|YY` (`60-72-stack.png`). Insert/delete through the DBNavigator buttons not exercised.

### 44. After every Execute the current cell is drawn as an empty inplace editor (a caret "I") that hides the value

Severity: unreadable (first value of every result hidden until the user clicks another cell).
Backend: both.
Steps: any query, Execute, look at row 1 / column 1.
Observed (`10-cell-c.png`, `11-combo.png`, `55-combo.png`): the first cell shows a white box with a text caret and no text - for the JOIN query the cell should read `Lord of the Rings - Part I`; clicking a cell in the grid (`13-combo.png`) paints the value with a focus rectangle. Same on SQLite (`55-combo.png`, all four queries).
Expected: the value.
What the code says (not verified): `DBGrid.Options` has `dgEditing` and `dgAlwaysShowSelection` (`src/EditorQuery.lfm`), the cells are painted by `DBGridDrawColumnCell` (`src/EditorQuery.pas:DBGridDrawColumnCell`, `DefaultDrawing=False`); the LCL `TDBGrid` shows its inplace editor for the focused cell when `dgEditing` is set and the grid has focus, and that editor apparently carries no text here (consistent with #43: the editor is empty and typing does not edit). Not compared with the original Delphi/CLX behaviour.
Evidence: `10-cell-c.png`, `11-combo.png`, `13-combo.png`, `55-combo.png`.
Suspected files: `src/EditorQuery.lfm` (DBGrid options), `src/EditorQuery.pas` (`DBGridDrawColumnCell`, `SizeGridCols`), `src/clx_shims/qdbgrids.pas`.
Status: fixed - `FormCreate` (`src/EditorQuery.pas`) added `dgAlwaysShowEditor` under `{$IFDEF LINUX}` (inherited from the CLX source, where the Qt grid needed it to be editable); the LCL `TDBGrid` then shows an empty `TStringCellEditor` over the focused cell after every Open (and `EditorIsReadOnly` puts the dataset into Edit/Insert straight away). The option is now removed at FormCreate; editing starts with F2 / a second click / a typed character. Verified on DISPLAY=:0 (`fix43/03-prod-c.png`, `30-43-stack.png`, `60-72-stack.png`): after every Execute all cells show their values, the current cell has the grey selection only; both backends.

### 45. DATE / DATETIME values are shown as `23-4-03` (day-month-two-digit-year, no zero padding, time dropped)

Severity: wrong data (a `2004-03-01` DATE reads as `1-3-04`; the DATETIME `2003-04-23 00:00:00` reads as `23-4-03`).
Backend: both (MySQL `15-combo.png` third row; SQLite `55-combo.png` third grid).
Steps: `SELECT o.*, c.name, c.creditcardnr, c.creditcarddate FROM onlineorder o LEFT JOIN onlinecustomer c ON c.idonlinecustomer=o.idonlinecustomer`, Execute.
Observed: `date` = `23-4-03`, `creditcarddate` = `1-3-04`; the CLI returns `2003-04-23 00:00:00` and `2004-03-01`.
Expected: ISO (or at least four-digit-year, zero-padded) display; the original DBDesigner 4 used the Windows/Qt locale.
What the code says: `DoFieldGetText` (`src/EditorQuery.pas`) sets the display text to `TField(Sender).AsString`, i.e. `DateTimeToStr` with `DefaultFormatSettings`; the port sets `DefaultFormatSettings.DecimalSeparator` in places (`src/DBEERDM.pas:2564`, `:3297`) but never `ShortDateFormat`/`LongTimeFormat` (grep over `src/`), so the FPC defaults apply. Not verified which FPC default produces `d-m-yy`.
Evidence: `15-combo.png`, `55-combo.png`.
Suspected files: `src/EditorQuery.pas` (`DoFieldGetText`, `OutputClientDataSetAfterOpen`), `src/DBDM.pas` (connection setup, format settings).

### 46. A query that returns no rows shows one phantom empty row (with the #44 editor) in the grid

Severity: functional/low (the status bar is right, the grid is not).
Backend: both.
Steps: `SELECT * FROM product WHERE name='zzz'`, Execute.
Observed (`15-combo.png` first block, `55-combo.png` fourth block): status "Query opened. 0 Record(s) fetched", the grid shows the 7 column titles and one row with the indicator and the empty inplace editor in column 1, as if an insert row existed.
Expected: an empty grid below the titles.
Suspected files: same as #44 (`dgEditing` on an empty `TBufDataset` - the LCL grid may show an append row); not verified.
Status: fixed - the LCL `TCustomDBGrid.UpdateGridCounts` always allocates one data row (`if RecCount<1 then RecCount := 1`), so an empty dataset shows a placeholder row; with `dgAlwaysShowEditor` (#44) that row also went into Insert. `SizeGridCols` now drops `dgIndicator` while the result has 0 records (restored on the next non-empty result) and `DBGridDrawColumnCell` paints the placeholder row blank, so the grid shows only the column titles. Verified on DISPLAY=:0 (`fix43/20-22-stack.png` MySQL, `60-72-stack.png` SQLite): titles, empty area, status "0 Record(s) fetched".

### 47. (unconfirmed, intermittent) One `SELECT * FROM product` right after a syntax-error dialog returned "1 Record(s) fetched" with an empty one-column grid

Severity: wrong data if reproducible.
Backend: MySQL.
Steps: `SELEC * FROM product` -> error dialog, OK; click the SQL memo, Ctrl+A, type `SELECT * FROM product`, Execute.
Observed (`17-prod.png`, `19-main.png`, `19-q-combo.png`): memo text `SELECT * FROM product`, status "Query opened. 1 Record(s) fetched. Time: 00:00:017", grid with one untitled column and one empty row. The same sequence repeated later (`22-err` -> `23-after`, `24-after2`) gave the correct 3 rows every time (`24-all.png`), and the SQLite run (`54-serr` -> `55-safter`) too. `run.log` empty. Possibly a lost keystroke (the 19 screenshot was taken after a further click/typing attempt, `19-q-combo.png` shows `productXX` in the memo), so the executed text is not certain.
Next step: repeat with a `writeln` of `OutputQry.SQL.Text` and `RecordCount` in `ExecSQLBtnClick`.
Suspected files: `src/EditorQuery.pas:963-1128` (`ExecSQLBtnClick`, the `except` branch leaves `OutputClientDataSet` in whatever state the failed `Open` left it), `src/clx_shims/dbclient.pas`.
Status: not reproduced in 5 attempts (3 on MySQL `41-43-ok`, 2 on SQLite `71-72-sok`, `fix43/30-43-stack.png`, `60-72-stack.png`): `SELEC * FROM product` -> error dialog -> OK -> `SELECT * FROM product` gave the full 3x7 result and "3 Record(s) fetched" every time, with the #43/#44/#46 changes in place. One different oddity seen once (`fix43/60-sprod.png`): the first Execute right after switching the connection from MySQL to SQLite left the grid in its inactive look (indicator + one untitled cell) and the status bar unchanged (still "Changes applied ..."), no error dialog, memo text intact; the second Execute worked. Not chased within the time box - if it recurs, `writeln` the branch taken in `ExecSQLBtnClick` (`DMDB.CurrentDBConn`, `GetSQLMemoText`) right after a reconnect.

### 48. Ctrl+S in Query mode does not save the model (File > Save does)

Severity: functional (the user thinks the stored SQL / history is saved).
Backend: n/a (model file).
Steps: in Query mode, with the last click in the SQL memo or the stored-SQL tree, press Ctrl+S; check the file.
Observed: `order.xml` mtime unchanged (19:03:59, the copy time) after two Ctrl+S presses at 19:11-19:12 while the model had changed (new stored SQL, history entries; `31-tree-c.png`); File > Save at 19:12:39 wrote the file with the `Round11/prod` record and the six `SQLCmdType="4"` history records of the session, status "The model was successfully saved to ..." (`36-combo.png` bottom line). Ctrl+S in Design mode worked in every earlier round.
Expected: Ctrl+S saves from any focused control, as the menu shortcut of `SaveMI`.
What the code says (not verified): `SaveMI` (`src/Main.pas:106`, `SaveMIClick` `:742`) is a main-menu item; the docked `TEditorQueryForm` has its own `FormKeyDown` (`src/EditorQuery.pas:2511`, F1 only) and a `TMemo` that swallows Ctrl+S under GTK2 (`gtk_text_view` binds nothing to it, but the LCL delivers the key to the focused widget first). Whether the shortcut is registered on the main form's menu under the LCL when a docked form owns the focus was not read.
Evidence: `order.xml` mtime, `31-tree-c.png`, `36-combo.png`.
Suspected files: `src/Main.pas` (`SaveMI` shortcut, `FormKeyDown` `:3615`), `src/EditorQuery.pas` (`FormKeyDown`, `SQLMemo`).

### 49. Renaming a stored SQL command opens a dialog titled "Connecion Name"

Severity: cosmetic (wrong, misspelt title; the dialog itself works).
Backend: n/a.
Steps: select the stored node `Round11/prod` in the tree and click it again (or double-click).
Observed (`36-combo.png`, `65-combo.png`, window title in `wins`: "Connecion Name", 313x61): a "Name:" string editor prefilled with `prod`; Return with `prod2` renames the node (`39-combo.png`). Escape does not close this dialog (only the red cross / Return do; `66`-`67` attempts), which blocked the popup-menu retry of #50.
Expected: a title like "Rename SQL Command", closable with Escape.
Cause (confirmed by reading): `src/EditorQuery.pas:1572` (`StoredSQLTreeViewEditing`) calls `DMMain.ShowStringEditor('Connecion Name', 'Name:', s)` - the literal was copied from `src/DBConnSelect.pas:521` (translated message 109). Escape handling is the string editor's (`DMMain.ShowStringEditor`, not read).
Evidence: `36-combo.png`, `65-combo.png`.
Suspected files: `src/EditorQuery.pas:1561-1583`, `src/MainDM.pas` (`ShowStringEditor`).

### 50. (unconfirmed) Stored-SQL tree popup "Execute SQL Command" / "Delete Item(s)" did nothing in three attempts

Severity: functional if confirmed.
Backend: MySQL (Execute), none needed (Delete).
Steps: right-click the node `Round11/prod2` (screen 150,740), popup `Execute SQL Command / Edit SQL Command / Delete Item(s) / Refresh Tree` (`39-combo.png`, 211x110 at 150,740); click Execute (250,755) with an empty SQL memo; later click Delete Item(s) (250,807 / 230,806).
Observed: after Execute the memo stayed empty and the status bar kept "The model was successfully saved ..." (`40-combo.png`, `41-query-c.png`), i.e. `StoredSQLExecuteBtnClick` (`src/EditorQuery.pas:1512`, which first copies the node's SQL into the memo via `StoredSQLEditBtnClick`) did not run or found `StoredSQLTreeView.Selected=nil`; after Delete the node was still there in the fresh session both without (`61-tree-c.png`) and with (`62-combo.png` right) a prior left click on the node; the last two retries were blocked by the modal #49 dialog, so a click landing outside the popup items cannot be excluded (the popup was never captured after the click).
What the code says: `DeleteSQLCommandMIClick` (`:1593`) deletes only nodes with `Selected=True` and `Data<>nil`, `DeleteSQLCommandMIShow` (`:1584`) disables the item when nothing is selected; `RefreshStoredSQLTreeView` rebuilds the tree after a rename/store, which drops the selection - so a right-click on an unselected node (LCL `TTreeView` does not select on right click unless `RightClickSelect`) gives a disabled/no-op Delete. Whether that is the whole story needs a retry with a verified popup capture and a `writeln` in both handlers.
Evidence: `40-combo.png`, `41-combo.png`, `61-tree-c.png`, `62-combo.png`.
Suspected files: `src/EditorQuery.pas:1504-1520`, `:1584-1620`, `RefreshStoredSQLTreeView` (`:1341`), `src/EditorQuery.lfm` (StoredSQLTreeView `RightClickSelect`).

Round 11 OK (not entries): the Query Drag Target form and its seven drop zones render completely (`07-dt-4200045.png`); JOIN with alias + WHERE string literal + ORDER BY DESC returns the CLI's rows in the CLI's order on both backends; NULL TEXT/BLOB (`info`, `pic`) shown as empty cells; wide result scrolls horizontally (horizontal scrollbar under the grid); the error dialog shows the statement and the driver message and the connection survives on both backends; the grid popup and the "Copy to Clipboard" submenu exist; stored SQL, its folder and the History entries (`SQLCmdType="4"`, `DD.MM HH:NN:SS` names, 20-entry cap in `ExecSQLBtnClick`) round-trip through File > Save and reopen; Design/Query mode switch keeps tree, memo and grid; disconnect and reconnect to the other backend work without restart. Observations without entry: DECIMAL(10,2) `14.20` is displayed as `14.2` (float conversion in the shim; SQLite stores it as `real` anyway); once, after `Execute SQL Command` from the popup, row 1 of the grid was painted in a smaller font than rows 2-3 (`41-combo.png` - a repaint artefact after focus changes, seen only once); `Store SQL Command` dialog has no visible title text in the capture (WM frame only).

Exercised / not reached: exercised - stages 1, 2, 6 on both backends, 3-5 on MySQL as listed in the stage table. Not reached - column resize, header-click sort, Ctrl+C from the grid (no clipboard tool on the machine), "Export Records" / "Print Records to PDF", BLOB viewer / Load-Store BLOB buttons (all BLOB values NULL in the example data), UPDATE/INSERT/DELETE statements through the memo (`ExecuteSQLCmdScript` branch), multi-statement scripts, "Scripts" and "Table Selects" nodes, the temporary SQL store buttons, layout switch button, SQL history navigation (`PrevCmdClick`), Load SQL Script from File, Cut/Paste in the memo, stored-SQL rename persistence (renamed `prod2` was not saved again: the reopened model shows `prod`, as expected).

Proposed grouping for this pass: (A) result grid inplace editing - #43, #44, #46 (one LCL `TDBGrid` + `TBufDataset` shim issue: `dgEditing` with an editor that neither shows the value nor accepts input, phantom row on empty sets); (B) field display - #45 (date format) plus the DECIMAL trailing-zero note; (C) stored-SQL tree - #50, #49; (D) shortcut routing - #48; (E) unconfirmed result-state bug - #47. Recommended order: #47 (verify first: wrong data if real), #43/#44/#46 together, #45, #48, #50, #49.
